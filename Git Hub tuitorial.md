Repository add & configure Git username & email
git status #To check current status of repository
git init #To initialize the repository if not initialized yet
কোন Git রিপোজিটরিতে আছেন কি না বা আপনার বর্তমান ফোল্ডারটি একটি Git রিপোজিটরি কি না, তা চেক করার কয়েকটি সহজ উপায় আছে:

১. সবথেকে সহজ উপায়: git status
আপনার টার্মিনালে শুধু নিচের কমান্ডটি লিখুন:

Bash -> git status
যদি এটি রিপোজিটরি হয়: আপনি দেখতে পাবেন কোন ব্রাঞ্চে (Branch) আছেন এবং আপনার ফাইলগুলোর অবস্থা কী। (যেমন: On branch main)

যদি এটি রিপোজিটরি না হয়: টার্মিনালে এই এরর মেসেজটি আসবে:
fatal: not a git repository (or any of the parent directories): .git
২. হিডেন ফোল্ডার চেক করা (ম্যানুয়ালি)
প্রতিটি Git রিপোজিটরির ভেতরে একটি হিডেন (Hidden) ফোল্ডার থাকে যার নাম .git। আপনি টার্মিনাল থেকে এটি দেখতে এই কমান্ডটি দিতে পারেন:

Git Bash বা Linux-এ: ls -a
Windows CMD-তে: dir /ah

যদি লিস্টে .git ফোল্ডারটি দেখেন, তবে বুঝবেন এটি একটি লোকাল রিপোজিটরি।

৩. রিমোট লিঙ্ক চেক করা (কোন GitHub লিঙ্কের সাথে যুক্ত)
আপনি যদি দেখতে চান এই ফোল্ডারটি GitHub-এর কোন নির্দিষ্ট রিপোজিটরির সাথে কানেক্ট করা আছে, তবে লিখুন:
Bash -> git remote -v

যদি এটি কোনো অনলাইন রিপোজিটরির সাথে যুক্ত থাকে, তবে তার URL (লিঙ্ক) এখানে দেখাবে। যদি কিছুই না দেখায়, তার মানে এটি শুধুমাত্র আপনার পিসিতেই আছে, অনলাইনে লিঙ্ক করা নেই।

## repository link add করা

১. লিঙ্কটি কোথায় পাবেন?
প্রথমে GitHub-এ গিয়ে একটি নতুন রিপোজিটরি (New Repository) তৈরি করুন। তৈরি করার পর সেখানে HTTPS বা SSH ট্যাব থেকে একটি লিঙ্ক পাবেন, যা দেখতে অনেকটা এরকম: https://github.com/your-username/your-repo-name.git

২. লোকাল প্রজেক্টের সাথে লিঙ্ক করার নিয়ম
আপনার টার্মিনালে প্রজেক্ট ফোল্ডারের ভেতর থেকে নিচের ধাপগুলো ফলো করুন:
গিট শুরু করা (যদি আগে না করে থাকেন):

Bash -> git init
GitHub-এর লিঙ্কটি সেট করা (এটিই আসল কাজ):

Bash -> git remote add origin https://github.com/your-username/your-repo-name.git
(এখানে origin হলো আপনার অনলাইনের লিঙ্কটির একটি সংক্ষিপ্ত নাম)

৩. লিঙ্ক হয়েছে কি না চেক করা
আপনার লোকাল প্রজেক্টটি কোন লিঙ্কের সাথে কানেক্টেড আছে তা দেখতে এই কমান্ডটি দিন:

Bash -> git remote -v
যদি সবকিছু ঠিক থাকে, তবে আপনি আপনার GitHub রিপোজিটরির লিঙ্কটি দেখতে পাবেন।

## নতুন ব্রাঞ্চে কোড আপলোড (Push) করা

কখনো কখনো প্রয়োজন হয় নতুন একটি ব্রাঞ্চ (Branch) তৈরি করে সেখানে কোড আপলোড (Push) করার। নিচে ধাপে ধাপে সেই প্রক্রিয়া দেওয়া হলো:

১. নতুন ব্রাঞ্চ তৈরি করা এবং সেখানে যাওয়া
টার্মিনালে এই কমান্ডটি দিন (এখানে apnar-branch-name এর জায়গায় আপনার পছন্দমতো নাম দিন):

Bash -> git checkout -b apnar-branch-name
(এই কমান্ডটি একই সাথে একটি নতুন ব্রাঞ্চ তৈরি করবে এবং আপনাকে সেই ব্রাঞ্চে নিয়ে যাবে)

২. ফাইলগুলো অ্যাড করা
আপনার করা পরিবর্তনগুলো স্টেজিং এরিয়াতে যোগ করুন:

Bash

git add .
৩. পরিবর্তনগুলো কমিট করা
Bash -> git commit -m "apnar commit message"
৪. নতুন ব্রাঞ্চটি GitHub-এ পাঠানো (Push)
এখন কোডগুলো ওই নির্দিষ্ট ব্রাঞ্চে পাঠাতে নিচের কমান্ডটি দিন:

Bash -> git push -u origin apnar-branch-name
(এখানে apnar-branch-name এর জায়গায় আপনার তৈরি করা ব্রাঞ্চের নাম দিন)

কিছু জরুরি তথ্য:
ব্রাঞ্চ চেক করা: আপনি বর্তমানে কোন ব্রাঞ্চে আছেন তা দেখতে git branch লিখুন। বর্তমানে যে ব্রাঞ্চে আছেন তার পাশে একটি স্টার (\*) চিহ্ন থাকবে।

অন্য ব্রাঞ্চে যাওয়া: যদি আগে থেকে তৈরি কোনো ব্রাঞ্চে যেতে চান, তবে লিখুন: git checkout branch-name (এখানে -b লাগবে না)।
এখন GitHub-এ গিয়ে আপনার রিপোজিটরি চেক করলে দেখবেন সেখানে দুটি ব্রাঞ্চ আছে (main এবং আপনার তৈরি করা নতুনটি)।

যদি লিঙ্ক ভুল হয় বা পরিবর্তন করতে চান?
যদি দেখেন ভুল রিপোজিটরির সাথে লিঙ্ক করে ফেলেছেন, তবে এই কমান্ড দিয়ে লিঙ্কটি আপডেট করতে পারেন:

Bash -> git remote set-url origin https://github.com/your-username/new-repo-name.git

## git pull request

Pull Request (PR) হলো এমন একটি প্রক্রিয়া যার মাধ্যমে আপনি অন্যদের (বা মেইন প্রোজেক্টের মালিককে) জানান যে—আপনি কিছু পরিবর্তন করেছেন এবং আপনি চান সেই পরিবর্তনগুলো মেইন কোড বা main ব্রাঞ্চের সাথে যুক্ত (Merge) করা হোক।

আপনি যেহেতু নতুন ব্রাঞ্চে কোড পুশ করেছেন, এখন আপনি নিচের ২টি উপায়ে PR করতে পারেন:

পদ্ধতি ১: GitHub ওয়েবসাইট থেকে (সবচেয়ে সহজ)
১. আপনার GitHub রিপোজিটরিতে যান। ২. সেখানে একটি হলুদ রঙের ব্যানার দেখতে পাবেন যাতে লেখা আছে: "Compare & pull request"। ওই বাটনে ক্লিক করুন। ৩. এরপর একটি পেজ আসবে যেখানে:

base: main (যেখানে কোড যাবে)

compare: your-branch-name (যেখান থেকে কোড আসবে) ৪. একটি সুন্দর টাইটেল এবং আপনি কী কাজ করেছেন তার একটি ছোট বর্ণনা (Description) লিখুন। ৫. সবশেষে "Create pull request" বাটনে ক্লিক করুন।

পদ্ধতি ২: টার্মিনাল থেকে (GitHub CLI ব্যবহার করে)
আপনি যদি আগে GitHub CLI (gh) ইন্সটল করে থাকেন, তবে ব্রাউজারে না গিয়েই টার্মিনাল থেকে PR করতে পারেন:

১. টার্মিনালে লিখুন:

Bash

gh pr create --title "আপনার কাজের টাইটেল" --body "কাজের বর্ণনা"
২. এরপর কিছু অপশন আসতে পারে, সেখানে এন্টার চাপলে আপনার PR তৈরি হয়ে যাবে।

এর পরের কাজ কী?
PR তৈরি করার পর সাধারণত ৩টি জিনিস ঘটে:
Review: কেউ আপনার কোড চেক করে দেখবে কোনো ভুল আছে কি না।
Conflict Check: GitHub নিজে চেক করবে আপনার কোড মেইন ব্রাঞ্চের সাথে কোনো ঝামেলা করছে কি না।
Merge: সবকিছু ঠিক থাকলে আপনি নিজেই (বা অ্যাডমিন) "Merge pull request" বাটনে ক্লিক করে কোডগুলো main ব্রাঞ্চে ঢুকিয়ে দেবেন।

বোনাস টিপস:
আপনার কাজ শেষ হয়ে গেলে এবং কোড main-এ চলে গেলে, আপনি চাইলে আপনার তৈরি করা এক্সট্রা ব্রাঞ্চটি ডিলিট করে দিতে পারেন।

## ব্রাঞ্চ ডিলিট করার প্রক্রিয়াটি দুইভাবে করতে হয়—একটি আপনার নিজের পিসি (Local) থেকে এবং অন্যটি GitHub (Remote) থেকে। নিচে সহজ কমান্ডগুলো দেওয়া হলো:

১. লোকাল পিসি (Local) থেকে ব্রাঞ্চ ডিলিট করা
প্রথমে নিশ্চিত হয়ে নিন যে আপনি বর্তমানে সেই ব্রাঞ্চটিতে নেই যা আপনি ডিলিট করতে চান। অন্য কোনো ব্রাঞ্চে (যেমন: main) চলে যান:

Bash -> git checkout main
এখন নিচের কমান্ডটি দিন:

মার্জ করা হয়ে থাকলে (নিরাপদ উপায়):

Bash -> git branch -d branch-name
মার্জ না করলেও জোরপূর্বক ডিলিট করতে চাইলে:

Bash -> git branch -D branch-name
২. GitHub (Remote) থেকে ব্রাঞ্চ ডিলিট করা
GitHub সার্ভার থেকে ব্রাঞ্চটি মুছে ফেলতে টার্মিনালে এই কমান্ডটি দিন:

Bash -> git push origin --delete branch-name
৩. GitHub ওয়েবসাইট থেকে ডিলিট করা (বিকল্প উপায়)
আপনি চাইলে সরাসরি GitHub-এর ওয়েবসাইট থেকেও এটি করতে পারেন:

আপনার রিপোজিটরিতে যান।

"Branches" অপশনে ক্লিক করুন (যেখানে ব্রাঞ্চের সংখ্যা দেখায়)।

আপনার ব্রাঞ্চের নামের পাশে একটি ডাস্টবিন (Trash) আইকন দেখতে পাবেন, সেখানে ক্লিক করলেই ডিলিট হয়ে যাবে।

ডিলিট হয়েছে কি না চেক করার কমান্ড:
লোকাল ব্রাঞ্চ চেক করতে: git branch

রিমোট ব্রাঞ্চ চেক করতে: git branch -a

টিপস: কাজ শেষ হয়ে যাওয়ার পর ব্রাঞ্চ ডিলিট করে ফেলা ভালো, এতে রিপোজিটরি পরিষ্কার থাকে।

## Merge হয়েছে কি না এবং Pull Request (PR) সাকসেসফুল হয়েছে কি না, তা বোঝার সবচেয়ে সহজ উপায় হলো GitHub-এর ওয়েবসাইট চেক করা। তবে টার্মিনাল থেকেও এটি চেক করা যায়।

নিচে বিস্তারিত পদ্ধতি দেওয়া হলো:

১. GitHub ওয়েবসাইট দেখে বোঝা (সহজ উপায়)
আপনার GitHub রিপোজিটরির "Pull requests" ট্যাবে যান:

Open: যদি দেখেন আপনার PR-টি এখনো "Open" ট্যাবে আছে, তার মানে এটি এখনো Merge হয়নি।
Merged: যদি PR-এর পাশে বেগুনি রঙের (Purple) একটি আইকন এবং "Merged" লেখা দেখেন, তার মানে আপনার কাজ সফলভাবে main ব্রাঞ্চে ঢুকে গেছে।
Closed: যদি লাল রঙের "Closed" লেখা দেখেন, তার মানে PR-টি গ্রহণ না করেই বন্ধ করে দেওয়া হয়েছে।

২. টার্মিনাল (Git CLI) ব্যবহার করে চেক করা
আপনার পিসিতে টার্মিনাল ওপেন করে নিচের ধাপগুলো ফলো করুন:

প্রথমে main ব্রাঞ্চে যান:

Bash -> git checkout main
GitHub থেকে সব আপডেট নিয়ে আসুন:
Bash -> git pull origin main
এখন চেক করুন আপনার ব্রাঞ্চটি মার্জ হয়েছে কি না:

Bash -> git branch --merged
(এই লিস্টে যদি আপনার নতুন তৈরি করা ব্রাঞ্চটির নাম দেখতে পান, তবে বুঝবেন সেটি সফলভাবে main-এর সাথে মার্জ হয়ে গেছে।)

৩. GitHub CLI (gh) ব্যবহার করে চেক করা
যদি আপনি GitHub CLI ব্যবহার করেন, তবে খুব সহজেই স্ট্যাটাস দেখতে পারেন:

Bash -> gh pr status
এই কমান্ডটি দিলে আপনার করা সাম্প্রতিক PR-এর অবস্থা (Open, Merged, or Closed) সরাসরি টার্মিনালেই দেখিয়ে দেবে।

৪. কোড চেক করে নিশ্চিত হওয়া
আপনি সরাসরি main ব্রাঞ্চের ফাইলগুলো ওপেন করে দেখতে পারেন আপনার করা পরিবর্তনগুলো সেখানে আছে কি না। যদি কোডগুলো দেখতে পান, তবে বুঝবেন মার্জ হয়ে গেছে।

সংক্ষেপে চেনার উপায়:

সবুজ রঙ (Open): কাজ চলছে।

বেগুনি রঙ (Merged): অভিনন্দন! কাজ সফল।

লাল রঙ (Closed): ক্যানসেল করা হয়েছে।

আপনার কমান্ডগুলোর মানে:
cd ~: এটি আপনাকে আপনার পিসির Home Directory-তে (যেখানে Desktop, Documents ফোল্ডার থাকে) নিয়ে যাবে।
cd /d: এটি আপনাকে সরাসরি D Drive-এর একদম শুরুতে (Root) নিয়ে যাবে।
cd "L 4": এটি আপনাকে L 4 নামের ফোল্ডারে ঢুকিয়ে দেবে। নামের মাঝে স্পেস থাকলে এভাবে উদ্ধরণ চিহ্ন ("") দিতে হয়।
cd "../L 4/Intern":.. মানে এক ধাপ পেছনে যাওয়া।পুরোটার মানে: "এক ধাপ পেছনে যাও, তারপর সেখানে থাকা L 4 ফোল্ডারের ভেতর Intern ফোল্ডারে ঢোকো।"
cd ..: বর্তমানে যে ফোল্ডারে আছেন, সেখান থেকে ঠিক এক ধাপ পেছনে বা বাইরের ফোল্ডারে যাওয়ার জন্য এটি ব্যবহৃত হয়।

## আরও কিছু প্রয়োজনীয় টার্মিনাল কমান্ড

ls = বর্তমান ফোল্ডারে কী কী ফাইল বা ফোল্ডার আছে তার লিস্ট দেখা।
pwd = বর্তমানে আপনি ঠিক কোন ফোল্ডারের ভেতরে আছেন (পুরো পাথ) তা দেখা।
mkdir folder_name = নতুন একটি ফোল্ডার তৈরি করা।
touch file_name.txt = নতুন একটি ফাইল তৈরি করা।
clear = টার্মিনালের স্ক্রিন পরিষ্কার করা (অনেক লেখা হয়ে গেলে)।
mv file.txt folder/ = কোনো ফাইলকে এক জায়গা থেকে অন্য জায়গায় সরানো (Move)।
rm file.txt = কোনো ফাইল ডিলিট করা (সাবধানে ব্যবহার করবেন!)।
rm -rf folder_name = একটি পুরো ফোল্ডার এবং তার ভেতরের সব ডিলিট করা।

## Git username & email set করা

1.Global Git username & email set

    git config --global user.name "Sakif Shahrear"
    git config --global user.email "sakifshahrear@email.com"

## for chacking\*\*

    git config --global --list
    $ git config --global --list
    Output:
    user.email=sakifshahrear@gmail.com
    user.name=Sakif Shahrear
    filter.lfs.clean=git-lfs clean -- %f
    filter.lfs.smudge=git-lfs smudge -- %f
    filter.lfs.process=git-lfs filter-process
    filter.lfs.required=true

2. Local repository Git username & email set
   git config user.name "Sakif Shahrear"
   git config user.email "sakifshahrear@gmail.com"

## for chacking\*\*

    git config --list
    $ git config --list
    Output:
    $ git config --list

diff.astextplain.textconv=astextplain
filter.lfs.clean=git-lfs clean -- %f
filter.lfs.smudge=git-lfs smudge -- %f
filter.lfs.process=git-lfs filter-process
filter.lfs.required=true
http.sslbackend=openssl
http.sslcainfo=C:/Program Files/Git/mingw64/ssl/certs/ca-bundle.crt
core.autocrlf=true
core.fscache=true
core.symlinks=false
pull.rebase=false
credential.helper=manager
: ->> ** means there someting under which can't be shown here. for open terminal and type the command only q word. ** 3. Global config থেকে একটি value remove করা

email remove :

git config --global --unset user.email

# পুরো user section delete করা

git config --global --remove-section user

4. Make a small file change, stage it, view staged changes only.

staged command: git diff --staged (first git add korte hobe)
staged -> (state) which changes are added to the staging(state) area (git add) but not yet committed. (change ta git add kora hoyeche but git commit kora hoyni.)
আপনি যা নতুন যোগ করেছেন তা সবুজ রঙে (+) দেখাবে।

যা মুছে ফেলেছেন তা লাল রঙে (-) দেখাবে।

এটি শুধুমাত্র সেই পরিবর্তনগুলোই দেখাবে যেগুলো আপনি git add করেছেন কিন্তু এখনো commit করেননি।

    #include <bits/stdc++.h>
    using namespace std;
    +#define int long long

    -int main()
    +int32_t main()
    {
        int n;
        int x, y, z;
    @@ -9,10 +10,10 @@ int main()
    @@ -9,10 +10,10 @@ int main()

        for (int i = 0; i < n; i++)
        {
    -        int x;
    +        int x, count=0;
    +        int y;
            cin >> x;

    -        int count = 0;
            for (int j = 1; j * j <= x; j++)
            {
                if (x % j == 0)
    (END)
    # @@ -0,0 +1,36 @@: এর মানে ফাইলটি আগে খালি ছিল, এখন আপনি নতুন ১ থেকে ৩৬ নম্বর লাইন পর্যন্ত কোড যোগ করেছেন।

    কমান্ড,কী দেখায়?

5. View all changes (staged + unstaged) compared to last commit.

git diff ==== শুধুমাত্র সেই পরিবর্তনগুলো যেগুলো এখনো git add করা হয়নি (Unstaged)।
git diff --staged ==== শুধুমাত্র সেই পরিবর্তনগুলো যেগুলো git add করা হয়েছে (Staged)।
git diff HEAD ==== সবকিছুই দেখায় (Staged + Unstaged)। অর্থাৎ লাস্ট কমিটের পর থেকে আপনি যত যা করেছেন।

User@LAPTOP-4MIGM0J8 MINGW64 /d/coding/L 4/Intern (sakif_shahrear)
$ git diff head
diff --git a/L 4/Intern/one.txt b/L 4/Intern/one.txt
index 3b0980f..f9f6be8 100644
--- a/L 4/Intern/one.txt
+++ b/L 4/Intern/one.txt
@@ -11,7 +11,8 @@ int32_t main()
for (int i = 0; i < n; i++)
{
int x, count=0;

-        int y;

*        int y,m,n,o;
*        int change;
           cin >> x;

           for (int j = 1; j * j <= x; j++)

  diff --git a/L 4/Intern/phrase.md b/L 4/Intern/phrase.md
  index d7d5810..2a21fe3 100644
  --- a/L 4/Intern/phrase.md
  +++ b/L 4/Intern/phrase.md
  @@ -244,15 +244,51 @@ core.fscache=true
  core.symlinks=false
  pull.rebase=false
  credential.helper=manager
  -:
  +: ->> ** means there someting under which can't be shown here. for open terminal and type the command
  only q word. **

3.  Global config থেকে একটি value remove করা

email remove :

git config --global --unset user.email

-4️. পুরো user section delete করা
+# পুরো user section delete করা

git config --global --remove-section user

-5️.
\ No newline at end of file
+4. Make a small file change, stage it, view staged changes only.

- +staged command: git diff --staged (first git add korte hobe)
  +staged -> (state) which changes are added to the staging(state) area (git add) but not yet committed. (change ta git add kora hoyeche but git commit kora hoyni.)
  +আপনি যা নতুন যোগ করেছেন তা সবুজ রঙে (+) দেখাবে।
- +যা মুছে ফেলেছেন তা লাল রঙে (-) দেখাবে।
- +এটি শুধুমাত্র সেই পরিবর্তনগুলোই দেখাবে যেগুলো আপনি git add করেছেন কিন্তু এখনো commit করেননি।
- +5. View all changes (staged + unstaged) compared to last commit.
-
-
- # @@ -0,0 +1,36 @@: এর মানে ফাইলটি আগে খালি ছিল, এখন আপনি নতুন ১ থেকে ৩৬ নম্বর লাইন পর্যন্ত কোড যোগ গছেন।
  \ No newline at end of file
  (END)

6.  Create a new file, add content, commit it with a clear message.

    echo "This is a new file" > newfile.txt
    git add newfile.txt
    git commit -m "Add newfile.txt with initial content"
    or
    touch newfile.txt
    echo "This is a new file" >> newfile.txt (use >> to append otherwise it will overwrite mane dekhane na)
    git add newfile.txt
    git commit -m "Add newfile.txt with initial content"

7.  Rename a tracked file using Git (should stage automatically).
    git mv oldname.txt newname.txt
    git commit -m "Rename oldname.txt to newname.txt"

    proved :
    $ git mv divisor.cpp change_divisor.cpp

8.  Unstage a staged file without losing changes.
    cpmmand = git restore --staged <file_name>

    git status -> remove সবুজ (Green) নেই, বরং লাল (Red) হয়ে গেছে। এর মানে ফাইলটি এখন "Changes not staged for commit" অবস্থায় আছে। ২. আপনার ফাইলের ভেতরের কোনো কোড হারাবে না; It s possible to reopen and re edit আপনি চাইলে ফাইলটি ওপেন করে আবার এডিট করতে পারবেন।

    git add: ফাইলকে স্টেজ করার জন্য (Commit-এর লাইনে দাঁড় করানো)।
    git restore --staged: ফাইলকে আন-স্টেজ করার জন্য (লাইন থেকে বের করে দেওয়া)।
    Explaination:
    ১. git restore --staged <file> (লাইন থেকে বের করা)
    ধরুন, আপনি one.txt ফাইলটি এডিট করে git add করেছেন। এখন এটি Staging Area-তে (কমিট হওয়ার লাইনে) আছে। কিন্তু হুট করে আপনার মনে হলো, "না, এটা এখন কমিট করবো না, আরও কিছু কাজ বাকি আছে।"

        তখন আপনি দিবেন: git restore --staged one.txt

        ফলাফল: ফাইলটি স্টেজড থেকে আন-স্টেজড হয়ে যাবে।

        আপনার কোড: আপনার ফাইলের ভেতর যা যা লিখেছিলেন, সব ঠিকঠাক থাকবে, কিচ্ছু মুছবে না। শুধু গিট এটাকে "কমিট করার লিস্ট" থেকে সরিয়ে দিবে।

9.  Discard unstaged changes in a file.
    শুধু git restore <file> (কোড মুছে ফেলা - সাবধান!)
    যদি আপনি --staged কথাটি না লিখেন, তবে এর কাজ হবে একদম আলাদা। ধরুন, আপনি ফাইলে অনেক কিছু লিখেছেন কিন্তু সব ভুল হয়েছে। আপনি চাচ্ছেন ফাইলটিকে একদম সেই অবস্থায় ফিরিয়ে নিতে যেমনটা লাস্ট কমিটের সময় ছিল।

    তখন আপনি দিবেন: git restore one.txt

    ফলাফল: আপনার শেষ কমিটের পর থেকে ফাইলে যা যা নতুন লিখেছেন, সব মুছে যাবে এবং ফাইলটি আগের অবস্থায় ফিরে যাবে। (এটি খুব সাবধানে ব্যবহার করতে হয়)।

10. View commit history with one line per commit.
    git log --oneline
    proved:
    git log --oneline
    4613cd4 (HEAD -> sakif_shahrear, origin/sakif_shahrear) changed name
    bc4d863 divisor number
    d24a9a0 added some veriable in one.txt
    3295d8f added C++ basic structure in one.txt
    d812b20 start

11. View one-line history for a specific branch only.
    git log --oneline sakif_shahrear
    proved:
    4613cd4 (HEAD -> sakif_shahrear, origin/sakif_shahrear) changed name
    bc4d863 divisor number
    d24a9a0 added some veriable in one.txt
    3295d8f added C++ basic structure in one.txt
    d812b20 start

        git log --oneline --graph --all

১. git log --oneline --graph --all (সবথেকে পাওয়ারফুল)
এটি আপনার পুরো প্রোজেক্টের একটি "ম্যাপ" বা ম্যাপের মতো ছবি দেখাবে।

--oneline: প্রতিটি কমিটকে সংক্ষেপে এক লাইনে দেখাবে।

--graph: বাম পাশে লাঠির মতো বা ব্রাঞ্চের মতো দাগ টেনে দেখাবে কোন ব্রাঞ্চ কোথায় শুরু হয়েছে আর কোথায় গিয়ে মিশেছে।

--all: এটি খুব গুরুত্বপূর্ণ। এটি আপনার কম্পিউটারে থাকা সবগুলো ব্রাঞ্চের ইতিহাস একসাথে দেখাবে (শুধু বর্তমান ব্রাঞ্চেরটা নয়)।

কখন ব্যবহার করবেন: যখন আপনি দেখতে চান আপনার টোটাল প্রোজেক্টে কয়টি ব্রাঞ্চ আছে এবং সেগুলো একে অপরের সাথে কীভাবে যুক্ত।

২. git log --oneline sakif_shahrear (নির্দিষ্ট ব্রাঞ্চের জন্য)
এটি শুধুমাত্র একটি নির্দিষ্ট ব্রাঞ্চের ইতিহাস দেখাবে।

আপনি বর্তমানে যে ব্রাঞ্চেই থাকেন না কেন, এই কমান্ডটি দিলে শুধু sakif_shahrear ব্রাঞ্চে আপনি এ পর্যন্ত কী কী কমিট করেছেন তার লিস্ট দেখাবে।

এটি অন্য কোনো ব্রাঞ্চের (যেমন main বা master) কমিট দেখাবে না।

কখন ব্যবহার করবেন: যখন আপনি শুধু আপনার নিজের করা কাজের লিস্টগুলো এক নজরে দেখতে চান।

৩. git log --oneline (বর্তমান অবস্থার জন্য)
এটি আপনার বর্তমান ডিরেক্টরির বা বর্তমানে আপনি যে ব্রাঞ্চে দাঁড়িয়ে আছেন, শুধু তার ইতিহাস দেখাবে।

আপনি যদি এখন sakif_shahrear ব্রাঞ্চে থাকেন, তবে এটি আর আগের কমান্ডটি (git log --oneline sakif_shahrear) একই রেজাল্ট দিবে।

কিন্তু আপনি যদি ব্রাঞ্চ চেঞ্জ করে অন্য কোথাও যান, তখন এটি সেই নতুন ব্রাঞ্চের হিস্ট্রি দেখাবে।

কখন ব্যবহার করবেন: দ্রুত চেক করতে যে এই ব্রাঞ্চে আপনার শেষ কাজ কী ছিল।

git checkout main
git branch sakif1
git branch -m sakif
git branch -D sakif(not marged) if marged d replaced by D
remote branch -> git push origin --delete sakif (Remote server a ache)

যেহেতু ব্রাঞ্চটি সার্ভারে আছে, তাই নিচের এই কমান্ডটি ব্যবহার করুন:

git push origin --delete sakif (Remote server a ache)
২. যদি আপনি আপনার কম্পিউটারে থাকা sakif_shahrear ব্রাঞ্চটি মুছতে চান:

git branch -d sakif_shahrear (local server a ache amar pc te)
যদি এই ব্রাঞ্চের কাজগুলো main ব্রাঞ্চে মার্জ (Merge) করা না থাকে, তবে গিট আপনাকে এটি মুছতে দেবে না। সেক্ষেত্রে জোর করে মুছতে চাইলে ছোট হাতের -d এর বদলে বড় হাতের -D ব্যবহার করুন: git branch -D sakif_shahrear

১. আপনার ব্রাউজারে রিপোজিটরি খোলার জন্য:
আপনি যদি সরাসরি লিঙ্কে গিয়ে ফাইল বা ব্রাঞ্চ দেখতে চান, তবে এই কমান্ডটি দিলে আপনার ডিফল্ট ব্রাউজারে প্রজেক্টটি ওপেন হবে:

Bash => git remote -v
(এই কমান্ডটি দিলে আপনি আপনার রিপোজিটরির লিঙ্ক দেখতে পাবেন। সেটি কপি করে ব্রাউজারে পেস্ট করুন।)

২. রিমোট সার্ভারের ব্রাঞ্চগুলো দেখার জন্য:
সার্ভারে কী কী ব্রাঞ্চ আছে তা টার্মিনালেই দেখতে চাইলে লিখুন:

Bash => git branch -r
৩. সার্ভারের কোনো ব্রাঞ্চ নিজের কম্পিউটারে আনার জন্য (Checkout):
সার্ভারে থাকা কোনো ব্রাঞ্চে (যেমন sakif) কাজ করতে চাইলে সেটি আগে নিজের কম্পিউটারে ডাউনলোড করে নিতে হয়:

Bash => git checkout sakif
এই কমান্ডটি দিলে সার্ভারের sakif ব্রাঞ্চটি আপনার কম্পিউটারে চলে আসবে এবং আপনি তাতে কাজ করতে পারবেন।

যখন main ব্রাঞ্চ এবং আপনার বর্তমান ব্রাঞ্চের (sakif_shahrear) ইতিহাস বা শুরুটা এক জায়গা থেকে হয়নি। সহজ কথায়, গিট মনে করছে এই দুটি আলাদা প্রজেক্টের অংশ, তাই সে এগুলোকে মেলাতে (merge) দিচ্ছে না।

এটি সমাধান করার জন্য আপনাকে গিট-কে জোর করে বলতে হবে যে "অপ্রাসঙ্গিক ইতিহাস" (unrelated histories) থাকলেও মার্জ করো।

নিচের কমান্ডটি ব্যবহার করুন:

Bash => git merge main --allow-unrelated-histories
এই কমান্ডটি দিলে যা হতে পারে:
১. মার্জ হয়ে যাবে: যদি কোনো সমস্যা না থাকে তবে অটোমেটিক মার্জ হবে। ২. Conflict (সংঘাত) হতে পারে: যেহেতু দুটি ব্রাঞ্চের ইতিহাস আলাদা, তাই কিছু ফাইলে "Merge Conflict" আসতে পারে। সেক্ষেত্রে আপনাকে ফাইলগুলো ওপেন করে কোড ঠিক করে দিতে হবে। ৩. Editor ওপেন হতে পারে: মার্জ করার সময় একটি কমেন্ট লেখার জন্য আপনার কম্পিউটারের এডিটর (যেমন Vim বা Notepad) ওপেন হতে পারে। সেটি সেভ করে বন্ধ করলেই মার্জ সম্পন্ন হবে।

কেন এমন হলো?
হয়তো আপনি main ব্রাঞ্চ একভাবে শুরু করেছেন এবং sakif_shahrear ব্রাঞ্চটি অন্য কোনো ফোল্ডার বা সম্পূর্ণ আলাদাভাবে শুরু করেছিলেন।

অথবা, রিপোজিটরি ক্লোন করার সময় বা নতুন করে সেটআপ করার সময় দুই ব্রাঞ্চের শুরুর পয়েন্ট আলাদা ছিল।

User@LAPTOP-4MIGM0J8 MINGW64 /d/coding/L 4/Intern (feature-rebase|REBASE 2/2)
$ git checkout main

User@LAPTOP-4MIGM0J8 MINGW64 /d/coding/L 4/Intern (feature-rebase|REBASE 2/2)
$ git merge feature-A
Auto-merging L 4/Intern/conflict.txt
CONFLICT (content): Merge conflict in L 4/Intern/conflict.txt
Automatic merge failed; fix conflicts and then commit the result.
(.venv)
User@LAPTOP-4MIGM0J8 MINGW64 /d/coding/L 4/Intern (feature-rebase|REBASE 2/2)
$ git merge --abort
(.venv)
User@LAPTOP-4MIGM0J8 MINGW64 /d/coding/L 4/Intern (feature-rebase|REBASE 2/2)
$ git add "L 4/Intern/conflict.txt"
warning: could not open directory 'L 4/Intern/L 4/Intern/': No such file or directory
fatal: pathspec 'L 4/Intern/conflict.txt' did not match any files
(.venv)
User@LAPTOP-4MIGM0J8 MINGW64 /d/coding/L 4/Intern (feature-rebase|REBASE 2/2)
$ git rebase --continue
Successfully rebased and updated refs/heads/feature-rebase.
(.venv)

# Phase 1: Setup & Git Basics (সম্পূর্ণ গাইড)

## 🎯 লক্ষ্য

এই Phase-এ আপনি Git-এর মৌলিক কনফিগারেশন, staging, commit, এবং history management শিখবেন। শেষে আপনি ৩টি clean commit remote repository-তে push করবেন।

---

## প্রস্তুতি: Repository চেক করা

### কোন Git রিপোজিটরিতে আছেন কি না চেক করার উপায়:

**১. সবথেকে সহজ উপায়: `git status`**

```bash
git status
```

**ফলাফল:**

- ✅ **যদি রিপোজিটরি হয়:** `On branch main` দেখাবে
- ❌ **যদি রিপোজিটরি না হয়:** `fatal: not a git repository` error আসবে

**২. হিডেন ফোল্ডার চেক করা**

```bash
ls -a           # Git Bash/Linux
dir /ah         # Windows CMD
```

`.git` ফোল্ডার দেখলে বুঝবেন এটি Git repository।

**৩. রিমোট লিঙ্ক চেক করা**

```bash
git remote -v
```

GitHub লিঙ্ক দেখাবে যদি remote-এর সাথে যুক্ত থাকে।

---

## 📝 টাস্ক ১: Global Git Configuration

### ১.১ Global Username & Email সেট করা

**বাংলায়:** আপনার সব Git repository-তে ডিফল্ট নাম ও ইমেইল সেট করা।

```bash
git config --global user.name "Sakif Shahrear"
git config --global user.email "sakifshahrear@email.com"
```

**চেক করুন:**

```bash
git config --global --list
```

**আউটপুট:**

```
user.email=sakifshahrear@email.com
user.name=Sakif Shahrear
```

---

## 📝 টাস্ক ২: Local Git Configuration (Override)

### ২. Local Repository-তে আলাদা Email সেট করা

**বাংলায়:** নির্দিষ্ট একটি repository-তে আলাদা email ব্যবহার করা (যেমন: office project-এ office email)।

```bash
# Repository folder-এ যান
cd /d/coding/practice_repo

# Local config সেট করুন
git config user.name "Sakif Shahrear"
git config user.email "sakif@work.com"
```

**চেক করুন:**

```bash
git config --list
```

Local email global-কে override করবে শুধু এই repository-তে।

💡 **টিপস:** Output-এ `:` দেখলে `q` চাপুন বের হতে।

---

## 📝 টাস্ক ৩: Global Config Value Remove করা

### ৩. একটি নির্দিষ্ট Config Value মুছে ফেলা

**বাংলায়:** ভুল config সেট করে ফেললে মুছে ফেলার পদ্ধতি।

```bash
# Email remove করা
git config --global --unset user.email

# চেক করুন
git config --global --list
# দেখবেন user.email আর নেই
```

---

## 📝 টাস্ক ৪: পুরো User Section Remove করা

### ৪. সম্পূর্ণ User Section Delete করা

```bash
git config --global --remove-section user

# চেক করুন
git config --global --list
# user.name এবং user.email দুটোই নেই
```

⚠️ **সাবধান:** এর পর আবার নতুন করে username ও email সেট করতে হবে!

---

## 📝 টাস্ক ৫: Staged Changes দেখা

### ৫. File Change করে Stage করা এবং Staged Changes দেখা

**Staged কী?**  
`staged` মানে changes যেগুলো `git add` করা হয়েছে কিন্তু `git commit` করা হয়নি।

```bash
# File edit করুন
echo "New line added" >> fileA.txt

# File stage করুন
git add fileA.txt

# শুধু staged changes দেখুন
git diff --staged
```

**আউটপুট ব্যাখ্যা:**

- ✅ **সবুজ (+):** নতুন যোগ করা content
- ❌ **লাল (-):** মুছে ফেলা content

**উদাহরণ:**

```diff
+#define int long long    <-- নতুন লাইন
-int main()               <-- পুরানো লাইন
+int32_t main()           <-- নতুন লাইন
```

---

## 📝 টাস্ক ৬: সব Changes একসাথে দেখা

### ৬. Staged + Unstaged সব Changes দেখা

**তিনটি কমান্ডের পার্থক্য:**

| কমান্ড              | কী দেখায়?                             |
| ------------------- | -------------------------------------- |
| `git diff`          | শুধু unstaged (যা `git add` করা হয়নি) |
| `git diff --staged` | শুধু staged (যা `git add` করা হয়েছে)  |
| `git diff HEAD`     | সবকিছু (staged + unstaged)             |

```bash
# সব changes দেখুন
git diff HEAD
```

💡 `@@ -0,0 +1,36 @@` মানে: ফাইলে ১-৩৬ লাইন নতুন যোগ হয়েছে।

---

## 📝 টাস্ক ৭: নতুন File তৈরি ও Commit

### ৭. New File Create এবং Clear Message দিয়ে Commit

**পদ্ধতি ১:**

```bash
echo "This is a new file" > newfile.txt
git add newfile.txt
git commit -m "feat: add newfile.txt with initial content"
```

**পদ্ধতি ২:**

```bash
touch newfile.txt
echo "Content here" >> newfile.txt
git add newfile.txt
git commit -m "feat: add newfile with content"
```

⚠️ **পার্থক্য:**

- `>` = overwrite (আগের content মুছে নতুন লেখে)
- `>>` = append (শেষে যোগ করে)

---

## 📝 টাস্ক ৮: File Rename করা

### ৮. Git Command দিয়ে File Rename

**বাংলায়:** `git mv` দিয়ে rename করলে automatically staged হয়।

```bash
git mv oldname.txt newname.txt
git commit -m "refactor: rename oldname to newname"
```

**বাস্তব উদাহরণ:**

```bash
$ git mv divisor.cpp change_divisor.cpp
$ git status
# On branch main
# Changes to be committed:
#   renamed:    divisor.cpp -> change_divisor.cpp

$ git commit -m "refactor: rename divisor file"
```

💡 **কেন `git mv`?** সাধারণ `mv` দিলে Git ভাবে file delete+create হয়েছে, history ভাঙে।

---

## 📝 টাস্ক ৯: Unstage করা (Changes রেখে)

### ৯. Staged File Unstage করা (Changes ঠিক রেখে)

**বাংলায়:** ভুলবশত `git add` করে ফেলেছেন? Changes রেখে শুধু unstage করুন।

```bash
# নতুন Git version (recommended)
git restore --staged filename.txt

# পুরানো পদ্ধতি
git reset HEAD filename.txt
```

**উদাহরণ:**

```bash
echo "Change" >> test.txt
git add test.txt
git status
# Changes to be committed (staged)

git restore --staged test.txt
git status
# Changes not staged (কিন্তু change আছে)
```

---

## 📝 টাস্ক ১০: Unstaged Changes Discard করা

### ১০. File-এর সব পরিবর্তন বাতিল করা

⚠️ **সাবধান:** এটি করলে changes **চিরতরে মুছে যাবে**!

```bash
# নতুন Git
git restore filename.txt

# পুরানো পদ্ধতি
git checkout -- filename.txt
```

**উদাহরণ:**

```bash
echo "Unwanted" >> test.txt
git status
# modified: test.txt

git restore test.txt
git status
# nothing to commit (ফাইল আগের অবস্থায়)
```

---

## 📝 টাস্ক ১১: Compact History দেখা

### ১১. One-Line Format-এ Commit History

```bash
# সাধারণ one-line
git log --oneline

# Graph সহ
git log --oneline --graph --all

# সর্বশেষ ৫টি commit
git log --oneline -5
```

**আউটপুট:**

```
a1b2c3d (HEAD -> main) feat: add login
e4f5g6h fix: resolve bug
i7j8k9l docs: update README
```

---

## 📝 টাস্ক ১২: Specific Branch History

### ১২. নির্দিষ্ট Branch-এর History দেখা

```bash
# Main branch
git log --oneline main

# Feature branch
git log --oneline feature-login

# Current branch
git log --oneline
```

---

## 🎯 চূড়ান্ত চ্যালেঞ্জ: ৩টি Clean Commit Push

### Goal: ৩টি Commit তৈরি করে Remote-এ Push করা

**ধাপ ১: Feature 1 Commit**

```bash
echo "Login system implemented" > feature1.txt
git add feature1.txt
git commit -m "feat: add login feature"
```

**ধাপ ২: Feature 2 Commit**

```bash
echo "User management module" > feature2.txt
git add feature2.txt
git commit -m "feat: add user management"
```

**ধাপ ৩: Documentation Commit**

```bash
cat > README.md << 'EOF'
# My Practice Repository

This repo is for learning Git basics.

## Features
- Feature 1: Login system
- Feature 2: User management
EOF

git add README.md
git commit -m "docs: add comprehensive README"
```

**ধাপ ৪: Push করুন**

```bash
# Current branch চেক করুন
git branch
# * main

# Push করুন
git push origin main
```

**ধাপ ৫: যাচাই করুন**

```bash
# Local history দেখুন
git log --oneline

# Expected:
# xyz123 docs: add comprehensive README
# abc456 feat: add user management
# def789 feat: add login feature

# GitHub-এ গিয়ে verify করুন
```

---

## ✅ Phase 1 চেকলিস্ট

সব টাস্ক complete করেছেন কি?

- [ ] Global Git username ও email configure করা
- [ ] Local repository-তে আলাদা email override করা
- [ ] Global ও local config verify করা (`git config --list`)
- [ ] Global config value remove করা (`--unset`)
- [ ] পুরো user section remove করা (`--remove-section`)
- [ ] File change করে stage করা এবং staged changes দেখা
- [ ] Staged + unstaged সব changes একসাথে দেখা
- [ ] নতুন file তৈরি করে clear message দিয়ে commit করা
- [ ] Git command দিয়ে file rename করা (`git mv`)
- [ ] Staged file unstage করা (changes রেখে)
- [ ] Unstaged changes discard করা
- [ ] Compact one-line history দেখা
- [ ] Specific branch-এর history দেখা
- [ ] অন্তত ৩টি clean commit remote-এ push করা ✨

---

## ⚠️ সাধারণ ভুল এবং সমাধান

### ভুল ১: Commit Message না দেওয়া

```bash
# ❌ ভুল
git commit

# ✅ সঠিক
git commit -m "feat: add new feature"
```

### ভুল ২: সব ফাইল একসাথে Add করা

```bash
# ⚠️ সাবধান
git add .    # সব ফাইল (temp files সহ)

# ✅ ভালো
git add file1.txt file2.txt
git add src/*.js
```

### ভুল ৩: Push আগে Pull না করা

```bash
# ✅ সঠিক
git pull origin main    # আগে pull
git push origin main    # তারপর push

# ❌ ভুল
git push origin main    # conflict হতে পারে!
```

### ভুল ৪: Wrong Branch-এ Commit

```bash
# ✅ আগে চেক করুন
git branch
# * main <-- এখানে আছেন

# ভুল branch হলে switch করুন
git checkout correct-branch
```

---

## 📚 দ্রুত রেফারেন্স টেবিল

| কমান্ড                                  | কাজ              | উদাহরণ                                        |
| --------------------------------------- | ---------------- | --------------------------------------------- |
| `git config --global user.name "Name"`  | Global username  | `git config --global user.name "Sakif"`       |
| `git config --global user.email "mail"` | Global email     | `git config --global user.email "s@mail.com"` |
| `git config user.email "mail"`          | Local email      | `git config user.email "work@co.com"`         |
| `git config --list`                     | All configs      | `git config --global --list`                  |
| `git config --global --unset key`       | Remove config    | `git config --global --unset user.email`      |
| `git diff`                              | Unstaged changes | `git diff`                                    |
| `git diff --staged`                     | Staged changes   | `git diff --staged`                           |
| `git diff HEAD`                         | All changes      | `git diff HEAD`                               |
| `git add file`                          | Stage file       | `git add test.txt`                            |
| `git add .`                             | Stage all        | `git add .`                                   |
| `git commit -m "msg"`                   | Commit           | `git commit -m "feat: login"`                 |
| `git mv old new`                        | Rename file      | `git mv app.js main.js`                       |
| `git restore --staged file`             | Unstage          | `git restore --staged test.txt`               |
| `git restore file`                      | Discard changes  | `git restore test.txt`                        |
| `git log --oneline`                     | Compact history  | `git log --oneline -10`                       |
| `git log --oneline branch`              | Branch history   | `git log --oneline main`                      |
| `git push origin branch`                | Push to remote   | `git push origin main`                        |
| `git pull origin branch`                | Pull from remote | `git pull origin main`                        |
| `git status`                            | Check status     | `git status`                                  |
| `git remote -v`                         | Check remote     | `git remote -v`                               |

---

## 🎓 Phase 1 সম্পন্ন! অভিনন্দন! 🎉

আপনি এখন জানেন:

✅ কিভাবে Git configure করতে হয়  
✅ কিভাবে staging area কাজ করে  
✅ কিভাবে commit তৈরি এবং push করতে হয়  
✅ কিভাবে history দেখতে হয়  
✅ কিভাবে ভুল ঠিক করতে হয়

**পরবর্তী পদক্ষেপ:** 👉 **Phase 2: Branching & Daily Sync Workflow**

---

## 🔧 বোনাস: Terminal Navigation

### পাথ নেভিগেশন

```bash
cd ~                    # Home directory
cd /d                   # D Drive root
cd "L 4"                # Folder with space
cd "../L 4/Intern"      # Relative path
cd ..                   # One level up
pwd                     # Current location
```

### ফাইল ম্যানেজমেন্ট

```bash
ls                      # List files
ls -a                   # Show hidden files
mkdir folder            # Create folder
touch file.txt          # Create file
rm file.txt             # Delete file (careful!)
rm -rf folder/          # Delete folder (very careful!)
mv file.txt dest/       # Move file
cp file.txt backup.txt  # Copy file
clear                   # Clear terminal
cat file.txt            # Show file content
```

### Git Shortcuts

```bash
git status              # চেক করুন status
git branch              # চেক করুন branch
git remote -v           # চেক করুন remote
git log --oneline -5    # সর্বশেষ ৫ commits
```

---

## 📖 Additional Resources

### Repository Link Add করা

```bash
# GitHub-এ repo তৈরি করার পর
git init
git remote add origin https://github.com/username/repo.git
git remote -v    # Verify
```

### নতুন Branch-এ Code Push

```bash
git checkout -b feature-branch
git add .
git commit -m "feat: new feature"
git push -u origin feature-branch
```

### Branch Delete করা

```bash
# Local delete (merged)
git branch -d branch-name

# Local force delete
git branch -D branch-name

# Remote delete
git push origin --delete branch-name
```

---

**🎯 Ready for Phase 2?** Phase 2-তে আপনি branching, merging, এবং team collaboration শিখবেন!

# Phase 2: Branching & Daily Sync Workflow (ব্রাঞ্চিং এবং দৈনিক কাজের প্রবাহ)

## 🎯 লক্ষ্য

এই Phase-এ আপনি Git branching, branch management, এবং professional daily workflow শিখবেন। লক্ষ্য হলো একটি পূর্ণ দিনের simulation সফলভাবে complete করা।

---

## 📝 টাস্ক ১: নতুন Feature Branch তৈরি

### ১. Main থেকে নতুন Feature Branch তৈরি করা

**বাংলায়:** যখন নতুন feature নিয়ে কাজ করবেন, একটি আলাদা branch তৈরি করুন যাতে main branch clean থাকে।

```bash
# নিশ্চিত করুন আপনি main branch-এ আছেন
git checkout main

# Latest code pull করুন
git pull origin main

# নতুন feature branch তৈরি করুন
git checkout -b feature-login

# অথবা দুটি আলাদা কমান্ড
git branch feature-login
git checkout feature-login
```

**Verify করুন:**

```bash
git branch
# Output:
#   main
# * feature-login  <-- তারকা চিহ্ন মানে বর্তমান branch
```

💡 **Naming Convention:**

- `feature/login` - নতুন feature
- `bugfix/auth-error` - bug fix
- `hotfix/critical-security` - জরুরি fix
- `dev/sakif-experiment` - experimental কাজ

---

## 📝 টাস্ক ২: Branch Rename করা

### ২. বর্তমান Branch-এর নাম পরিবর্তন

**বাংলায়:** আপনি branch তৈরি করার পর মনে হলো নামটা আরও বর্ণনামূলক করা দরকার।

```bash
# নিশ্চিত করুন আপনি সেই branch-এ আছেন যেটা rename করবেন
git branch
# * feature-login

# Branch rename করুন
git branch -m feature-user-authentication

# Verify করুন
git branch
# * feature-user-authentication
```

**Remote-এ push করা থাকলে:**

```bash
# পুরানো branch remote থেকে delete
git push origin --delete feature-login

# নতুন নাম push করুন
git push origin -u feature-user-authentication
```

---

## 📝 টাস্ক ৩: পুরানো Merged Branch Delete

### ৩. Merge হয়ে যাওয়া পুরানো Branch মুছে ফেলা

**বাংলায়:** কাজ শেষ হয়ে merge হয়ে গেলে পুরানো branch রাখার দরকার নেই। Repository clean রাখুন।

**প্রথমে দেখুন কোন branches আছে:**

```bash
# Local branches
git branch

# Merged branches দেখুন
git branch --merged
```

**⚠️ সাবধান:** আপনি যে branch-এ আছেন সেটা delete করতে পারবেন না!

```bash
# Main branch-এ switch করুন
git checkout main

# Merged branch delete করুন (safe)
git branch -d feature-old-work

# Force delete (যদি merge না হয়ে থাকে)
git branch -D feature-experimental
```

**Remote থেকেও delete করুন:**

```bash
git push origin --delete feature-old-work
```

**Verify করুন:**

```bash
git branch
git branch -r    # Remote branches দেখুন
```

---

## 📝 টাস্ক ৪: Full Daily Start Simulation

### ৪. একটি পূর্ণ দিনের শুরুর প্রবাহ

**বাংলায়:** প্রতিদিন কাজ শুরু করার সময় এই steps follow করুন।

**ধাপ ১: Main Branch Update করুন**

```bash
# Main branch-এ যান
git checkout main

# Remote থেকে latest changes নিন
git pull origin main

# History দেখুন
git log --oneline -5
```

**ধাপ ২: Feature Branch-এ Switch করুন**

```bash
# আপনার feature branch-এ যান
git checkout feature-user-auth

# অথবা নতুন branch তৈরি করুন
git checkout -b feature-user-auth
```

**ধাপ ৩: Feature Branch Update করুন Main থেকে**

```bash
# Main-এর latest changes আপনার branch-এ নিয়ে আসুন
git merge main

# অথবা rebase করুন (cleaner history)
git rebase main
```

**Complete Workflow:**

```bash
# 📥 Step 1: Update main
git checkout main
git pull origin main

# 🔄 Step 2: Switch to feature branch
git checkout feature-user-auth

# ⬆️ Step 3: Update feature with latest main
git merge main
# অথবা
git rebase main

# ✅ Ready to work!
```

---

## 📝 টাস্ক ৫: Feature Branch-এ Commits করা

### ৫. Feature Branch-এ ২-৩টি Commit তৈরি

**বাংলায়:** এখন আপনার feature নিয়ে কাজ করুন এবং meaningful commits করুন।

**Commit 1: Initial setup**

```bash
# Login form তৈরি করুন
echo "Login Form HTML" > login.html
git add login.html
git commit -m "feat(auth): add login form HTML structure"
```

**Commit 2: Add styling**

```bash
# CSS যোগ করুন
echo "Login Form Styles" > login.css
git add login.css
git commit -m "style(auth): add login form styling"
```

**Commit 3: Add functionality**

```bash
# JavaScript যোগ করুন
echo "Login Form Logic" > login.js
git add login.js
git commit -m "feat(auth): implement login form validation"
```

**History check করুন:**

```bash
git log --oneline -3
# abc123 feat(auth): implement login form validation
# def456 style(auth): add login form styling
# ghi789 feat(auth): add login form HTML structure
```

---

## 📝 টাস্ক ৬: Outdated Branch-এ Push Failure

### ৬. পুরানো Branch-এ Push করে Failure দেখা

**বাংলায়:** ইচ্ছাকৃতভাবে branch update না করে push করলে কী হয় তা দেখা।

**Scenario:**

```bash
# Main-এ কেউ কিছু push করেছে কিন্তু আপনি pull করেননি
# এখন আপনি আপনার changes push করার চেষ্টা করছেন

git push origin feature-user-auth
```

**Expected Error:**

```
! [rejected]        feature-user-auth -> feature-user-auth (non-fast-forward)
error: failed to push some refs to 'origin'
hint: Updates were rejected because the tip of your current branch is behind
hint: its remote counterpart. Integrate the remote changes (e.g. 'git pull')
hint: before pushing again.
```

**কেন এই Error?**

- আপনার local branch outdated
- Remote-এ নতুন commits আছে যা আপনার কাছে নেই
- Git automatic merge করতে পারছে না

---

## 📝 টাস্ক ৭: Outdated Branch Fix করা

### ৭. Push Failure ঠিক করে Successful Push

**বাংলায়:** Error fix করে successfully push করার পদ্ধতি।

**সমাধান ১: Pull তারপর Push (Recommended)**

```bash
# Remote থেকে latest changes নিন
git pull origin feature-user-auth

# যদি conflict না থাকে, automatic merge হবে
# এখন push করুন
git push origin feature-user-auth
```

**সমাধান ২: Pull with Rebase (Cleaner History)**

```bash
# Rebase করে pull করুন
git pull --rebase origin feature-user-auth

# Push করুন
git push origin feature-user-auth
```

**সমাধান ৩: Force Push (⚠️ সাবধান!)**

```bash
# শুধুমাত্র আপনার একার branch-এ ব্যবহার করুন
git push --force origin feature-user-auth

# অথবা safer version
git push --force-with-lease origin feature-user-auth
```

**⚠️ Force Push কখন করবেন:**

- ✅ শুধু আপনার personal feature branch-এ
- ✅ যখন নিশ্চিত যে remote-এর changes দরকার নেই
- ❌ কখনোই main/master branch-এ না
- ❌ team-এর shared branch-এ না

---

## 📝 টাস্ক ৮: Full Sync Cycle ৩ বার

### ৮. সম্পূর্ণ Workflow ৩ বার Practice করা

**লক্ষ্য:** নিচের পুরো cycle ৩ বার করুন যতক্ষণ না আপনি comfortable হন।

### 🔄 Cycle 1: Morning Work Session

**Step 1: Daily Start**

```bash
# Update main
git checkout main
git pull origin main

# Go to feature branch
git checkout feature-user-auth
git merge main    # অথবা: git rebase main
```

**Step 2: Work & Commit**

```bash
# কাজ করুন
echo "User registration form" > register.html
git add register.html
git commit -m "feat(auth): add user registration form"

# আরেকটি commit
echo "Form validation rules" > validation.js
git add validation.js
git commit -m "feat(auth): add form validation rules"
```

**Step 3: Update Before Push**

```bash
# আবার main update check করুন
git checkout main
git pull origin main

# Feature branch-এ ফিরে merge করুন
git checkout feature-user-auth
git merge main    # যদি main-এ নতুন কিছু থাকে
```

**Step 4: Push**

```bash
git push origin feature-user-auth
```

### 🔄 Cycle 2: Afternoon Work Session

```bash
# 1️⃣ Daily sync
git checkout main && git pull origin main
git checkout feature-user-auth && git merge main

# 2️⃣ More work
echo "Password reset functionality" > reset-password.html
git add reset-password.html
git commit -m "feat(auth): add password reset feature"

echo "Email notification" > email-service.js
git add email-service.js
git commit -m "feat(auth): add email notification service"

# 3️⃣ Update check
git checkout main && git pull origin main
git checkout feature-user-auth && git merge main

# 4️⃣ Push
git push origin feature-user-auth
```

### 🔄 Cycle 3: Evening Polish

```bash
# 1️⃣ Daily sync
git checkout main && git pull origin main
git checkout feature-user-auth && git rebase main  # Rebase for clean history

# 2️⃣ Polish & document
echo "# Authentication Module" > AUTH_README.md
echo "Complete user auth system" >> AUTH_README.md
git add AUTH_README.md
git commit -m "docs(auth): add authentication module documentation"

# 3️⃣ Update check
git checkout main && git pull origin main
git checkout feature-user-auth && git rebase main

# 4️⃣ Push
git push origin feature-user-auth
```

---

## ✅ Phase 2 সফলতার মাপকাঠি

আপনি Phase 2 complete করেছেন যদি:

- [ ] নতুন feature branch তৈরি করতে পারেন
- [ ] Branch rename করতে পারেন (local + remote)
- [ ] Merged branch delete করতে পারেন
- [ ] Full daily sync workflow করতে পারেন
- [ ] Feature branch-এ meaningful commits করতে পারেন
- [ ] Outdated branch-এর push failure বুঝতে পারেন
- [ ] Push failure fix করে successful push করতে পারেন
- [ ] পুরো sync → work → update → push cycle ৩ বার করেছেন
- [ ] কোন push failure ছাড়াই একটি পূর্ণ দিনের simulation complete করেছেন ✨

---

## 📚 দ্রুত রেফারেন্স

### Branch Management

| কমান্ড                        | কাজ                          |
| ----------------------------- | ---------------------------- |
| `git checkout -b branch-name` | নতুন branch তৈরি + switch    |
| `git branch`                  | সব local branches দেখা       |
| `git branch -r`               | সব remote branches দেখা      |
| `git branch -a`               | সব branches (local + remote) |
| `git branch -m new-name`      | বর্তমান branch rename        |
| `git branch -d branch-name`   | Merged branch delete         |
| `git branch -D branch-name`   | Force delete                 |
| `git checkout branch-name`    | Branch switch                |

### Daily Workflow

| কমান্ড                        | কাজ                     |
| ----------------------------- | ----------------------- |
| `git pull origin main`        | Remote থেকে update      |
| `git merge main`              | Main-এর changes merge   |
| `git rebase main`             | Main-এর উপর rebase      |
| `git push origin branch`      | Remote-এ push           |
| `git push --force-with-lease` | Safe force push         |
| `git fetch origin`            | Remote check (no merge) |

---

## 💡 Pro Tips

### Tip 1: Branch Naming

```bash
# ভালো নাম
feature/user-authentication
bugfix/login-validation
hotfix/security-patch

# খারাপ নাম
test
new-branch
my-work
```

### Tip 2: Fetch vs Pull

```bash
# Fetch = শুধু check করুন (safe)
git fetch origin

# Pull = fetch + merge (changes আসবে)
git pull origin main
```

### Tip 3: Check Before Push

```bash
# সবসময় push আগে এই checks করুন
git status              # Clean working directory?
git log --oneline -5    # Commits ঠিক আছে?
git pull origin main    # Main updated?
git push origin branch  # Then push
```

### Tip 4: Alias তৈরি করুন

```bash
# Git aliases for faster workflow
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.cm commit
git config --global alias.st status
git config --global alias.lg "log --oneline --graph --all"

# এখন ব্যবহার করুন
git co main
git br feature-new
git cm -m "message"
```

---

## ⚠️ সাধারণ ভুল

### ভুল ১: Main Branch-এ সরাসরি কাজ করা

```bash
# ❌ ভুল
git checkout main
echo "code" > file.txt
git commit -m "quick fix"

# ✅ সঠিক
git checkout -b hotfix/quick-fix
echo "code" > file.txt
git commit -m "quick fix"
```

### ভুল ২: Update না করে Push করা

```bash
# ❌ ভুল (outdated branch)
git commit -m "work"
git push origin feature

# ✅ সঠিক
git pull origin main
git commit -m "work"
git push origin feature
```

### ভুল ৩: Force Push Shared Branch-এ

```bash
# ❌ বিপজ্জনক
git push --force origin main

# ✅ নিরাপদ (শুধু নিজের branch)
git push --force-with-lease origin my-feature
```

---

## 🎯 Phase 2 সম্পন্ন! এখন আপনি...

✅ Professional branching strategy বুঝেন  
✅ Daily development workflow জানেন  
✅ Branch management-এ comfortable  
✅ Push conflicts handle করতে পারেন  
✅ Team environment-এ কাজ করতে ready

**পরবর্তী:** 👉 **Phase 3: Conflict Resolution (Merge & Rebase)**

Phase 3-এ আপনি conflict handle, merge vs rebase, এবং VS Code দিয়ে visual conflict resolution শিখবেন!

# Phase 3: Conflict Resolution (Merge & Rebase)

## 🎯 লক্ষ্য

এই Phase-এ আপনি Git-এর সবচেয়ে important skill শিখবেন: **Conflict Resolution**। আপনি merge conflicts handle করতে, merge vs rebase বুঝতে, এবং VS Code দিয়ে visually conflicts solve করতে পারবেন।

---

## 📝 টাস্ক ১: Conflicting Changes তৈরি

### ১. একই File/Line-এ দুই Branch-এ আলাদা Edit

**বাংলায়:** Conflict কী তা বোঝার জন্য ইচ্ছাকৃতভাবে conflict তৈরি করা।

**Scenario Setup:**

**Main Branch-এ Edit:**

```bash
# Main branch-এ যান
git checkout main

# একটি file তৈরি/edit করুন
echo "Feature from main branch" > feature.txt
git add feature.txt
git commit -m "feat: add feature from main"

# Remote-এ push করুন
git push origin main
```

**Feature Branch-এ Same Line Edit:**

```bash
# Feature branch তৈরি করুন
git checkout -b feature-conflict

# SAME file-এর SAME content edit করুন
echo "Feature from feature branch" > feature.txt
git add feature.txt
git commit -m "feat: add feature from feature branch"
```

**এখন Conflict Ready!** যখন merge করবেন, Git বুঝতে পারবে না কোনটা রাখবে।

---

## 📝 টাস্ক ২: Merge Conflict Handle

### ২. Feature Branch কে Main-এ Merge → Conflict Resolve

**বাংলায়:** দুই branch একসাথে আনার সময় conflict আসলে manually solve করা।

**Merge করুন:**

```bash
# Main branch-এ যান
git checkout main

# Feature branch merge করুন
git merge feature-conflict
```

**Expected Conflict Message:**

```
Auto-merging feature.txt
CONFLICT (content): Merge conflict in feature.txt
Automatic merge failed; fix conflicts and then commit the result.
```

**File-এ যা দেখবেন:**

```txt
<<<<<<< HEAD
Feature from main branch
=======
Feature from feature branch
>>>>>>> feature-conflict
```

**Conflict Markers বোঝা:**

- `<<<<<<< HEAD` = বর্তমান branch-এর content (main)
- `=======` = বিভাজক line
- `>>>>>>> feature-conflict` = merge হচ্ছে যে branch-এর content

**Manual Resolution:**

**Option 1: Main-এর content রাখুন**

```bash
# File edit করুন, markers মুছে শুধু এটা রাখুন:
echo "Feature from main branch" > feature.txt
```

**Option 2: Feature-এর content রাখুন**

```bash
echo "Feature from feature branch" > feature.txt
```

**Option 3: দুটোই রাখুন**

```bash
cat > feature.txt << 'EOF'
Feature from main branch
Feature from feature branch
EOF
```

**Conflict Resolve Complete করুন:**

```bash
# Resolved file add করুন
git add feature.txt

# Merge commit করুন
git commit -m "Merge feature-conflict: resolved by keeping both features"

# Verify করুন
git log --oneline --graph -5
```

---

## 📝 টাস্ক ৩: Merge Abort করা

### ৩. Conflict থাকা Merge বাতিল করা

**বাংলায়:** Merge conflict আসার পর মনে হলো এখন solve করতে চান না, তাহলে abort করুন।

**Scenario:**

```bash
# Main-এ যান
git checkout main

# Conflict করা branch merge করুন
git merge feature-conflict
# CONFLICT আসবে
```

**Abort করুন:**

```bash
# Merge বাতিল করুন
git merge --abort

# Everything back to normal
git status
# On branch main
# nothing to commit, working tree clean
```

💡 **কখন Abort করবেন:**

- Conflict অনেক বেশি এবং এখন সময় নেই
- ভুল branch merge করে ফেলেছেন
- আগে discussion/planning দরকার

---

## 📝 টাস্ক ৪: Rebase Conflict Handle

### ৪. Rebase করে Conflict Resolve

**বাংলায়:** Merge-এর বদলে rebase করলে cleaner history হয় কিন্তু conflict handling একটু আলাদা।

**Conflict Setup (নতুন scenario):**

```bash
# Main-এ নতুন commit
git checkout main
echo "Main branch update 1" >> app.txt
git add app.txt
git commit -m "update: main branch improvement"

# Feature branch তৈরি (পুরানো main থেকে)
git checkout -b feature-rebase HEAD~1

# Feature-এ same file edit
echo "Feature branch update 1" >> app.txt
git add app.txt
git commit -m "update: feature improvement"
```

**Rebase করুন:**

```bash
# Feature branch-এ থাকুন
git checkout feature-rebase

# Main-এর উপর rebase করুন
git rebase main
```

**Conflict আসবে:**

```
CONFLICT (content): Merge conflict in app.txt
error: could not apply abc123... update: feature improvement
Resolve all conflicts manually, mark them as resolved with
"git add/rm <conflicted_files>", then run "git rebase --continue".
```

**File Check করুন:**

```bash
cat app.txt
# দেখবেন conflict markers আছে
```

**Resolve করুন:**

```bash
# File edit করে conflict fix করুন
nano app.txt    # অথবা VS Code দিয়ে
# Markers মুছে content ঠিক করুন

# Resolved file add করুন
git add app.txt

# Rebase continue করুন
git rebase --continue
```

**Rebase Abort (যদি দরকার হয়):**

```bash
# Rebase বাতিল করতে
git rebase --abort
```

**Merge vs Rebase Conflict Resolution তুলনা:**

| Aspect              | Merge                 | Rebase                    |
| ------------------- | --------------------- | ------------------------- |
| **Resolve করার পর** | `git commit`          | `git rebase --continue`   |
| **Abort করতে**      | `git merge --abort`   | `git rebase --abort`      |
| **History**         | Merge commit তৈরি হয় | Linear history থাকে       |
| **Use Case**        | Shared branches       | Personal feature branches |

---

## 📝 টাস্ক ৫: VS Code Visual Conflict Resolution

### ৫. VS Code দিয়ে Conflict Visually Resolve করা

**বাংলায়:** VS Code-এ conflict handling খুবই সহজ - buttons click করেই solve করা যায়!

**Conflict তৈরি করুন:**

```bash
# Main branch
git checkout main
echo "User authentication from main" > auth.js
git add auth.js
git commit -m "feat: add auth from main"

# Feature branch
git checkout -b feature-auth
echo "User authentication from feature" > auth.js
git add auth.js
git commit -m "feat: add auth from feature"

# Merge attempt
git checkout main
git merge feature-auth
# CONFLICT!
```

**VS Code-এ Open করুন:**

```bash
# VS Code খুলুন
code .
```

**VS Code-এ যা দেখবেন:**

File `auth.js` খুললে দেখবেন:

```javascript
<<<<<<< HEAD (Current Change)
User authentication from main
=======
User authentication from feature
>>>>>>> feature-auth (Incoming Change)
```

**VS Code Conflict Resolution Buttons:**

VS Code conflict-এর উপরে 4টি button দেখাবে:

1. **Accept Current Change** - Main branch-এর content রাখুন
2. **Accept Incoming Change** - Feature branch-এর content রাখুন
3. **Accept Both Changes** - দুটোই রাখুন
4. **Compare Changes** - Side-by-side তুলনা করুন

**Button Click করে Resolve:**

**Option 1: Accept Current (Main-এর content)**

```
→ Click "Accept Current Change"
→ File automatically cleaned (markers removed)
```

**Option 2: Accept Incoming (Feature-এর content)**

```
→ Click "Accept Incoming Change"
→ Feature-এর content থাকবে
```

**Option 3: Accept Both**

```
→ Click "Accept Both Changes"
→ দুটো content-ই থাকবে
```

**Resolve Complete করুন:**

```bash
# VS Code-এ resolved file save করুন
# Source Control panel-এ যান (Ctrl+Shift+G)

# Resolved files stage করুন
# '+' icon click করুন অথবা terminal-এ:
git add auth.js

# Commit করুন
git commit -m "Merge feature-auth: accept both authentication methods"
```

**VS Code Git Panel ব্যবহার:**

```
1. Left sidebar-এ Source Control icon (Ctrl+Shift+G)
2. "Merge Changes" section-এ conflicted files দেখবেন
3. File click করে resolve করুন
4. Stage করুন (+ icon)
5. Commit message লিখে commit করুন
```

---

## 📝 টাস্ক ৬: Merge Commit in History

### ৬. Merge Commit History-তে দেখা

**বাংলায়:** Branch merge করলে একটি special "merge commit" তৈরি হয় যেখানে দুই branch একসাথে আসে।

**Setup:**

```bash
# Feature branch তৈরি
git checkout -b feature-merge-demo
echo "Feature 1" > file1.txt
git add file1.txt
git commit -m "feat: add feature 1"

echo "Feature 2" > file2.txt
git add file2.txt
git commit -m "feat: add feature 2"

echo "Feature 3" > file3.txt
git add file3.txt
git commit -m "feat: add feature 3"
```

**Merge করুন:**

```bash
# Main-এ merge করুন
git checkout main
git merge feature-merge-demo
```

**History দেখুন:**

```bash
git log --oneline --graph --all -10
```

**আউটপুট (Merge Commit থাকবে):**

```
*   a1b2c3d (HEAD -> main) Merge branch 'feature-merge-demo'
|\
| * d4e5f6g feat: add feature 3
| * h7i8j9k feat: add feature 2
| * l0m1n2o feat: add feature 1
|/
* p3q4r5s Previous main commit
```

**Merge Commit বৈশিষ্ট্য:**

- দুই parent commit থাকে
- Branch junction point দেখায়
- Team collaboration track করা যায়

---

## 📝 টাস্ক ৭: Rebase vs Merge History তুলনা

### ৭. Same Scenario: Rebase করে History তুলনা

**বাংলায়:** একই কাজ rebase দিয়ে করলে history একদম linear (straight line) হয়।

**Setup (Same as before):**

```bash
# Feature branch তৈরি
git checkout -b feature-rebase-demo
echo "Rebase Feature 1" > rfile1.txt
git add rfile1.txt
git commit -m "feat: rebase feature 1"

echo "Rebase Feature 2" > rfile2.txt
git add rfile2.txt
git commit -m "feat: rebase feature 2"

echo "Rebase Feature 3" > rfile3.txt
git add rfile3.txt
git commit -m "feat: rebase feature 3"
```

**Rebase করুন (Merge-এর বদলে):**

```bash
# Feature branch-এ rebase করুন
git checkout feature-rebase-demo
git rebase main

# তারপর main-এ fast-forward merge
git checkout main
git merge feature-rebase-demo
```

**History দেখুন:**

```bash
git log --oneline --graph --all -10
```

**আউটপুট (Linear History):**

```
* x9y8z7w (HEAD -> main, feature-rebase-demo) feat: rebase feature 3
* a8b7c6d feat: rebase feature 2
* e5f4g3h feat: rebase feature 1
* i2j1k0l Previous main commit
```

**কোন merge commit নেই! Straight line!**

### Merge vs Rebase তুলনা

| Feature      | Merge                       | Rebase                       |
| ------------ | --------------------------- | ---------------------------- |
| **History**  | Branching (merge commit সহ) | Linear (straight line)       |
| **Pros**     | 🟢 True history preserve    | 🟢 Clean, readable history   |
|              | 🟢 Safe for shared branches | 🟢 Easy to understand flow   |
| **Cons**     | 🔴 Complex graph            | 🔴 Rewrites commit history   |
|              | 🔴 Many merge commits       | 🔴 Risky for shared branches |
| **Use When** | 🎯 Main branches            | 🎯 Personal feature branches |
|              | 🎯 Team collaboration       | 🎯 Before creating PR        |
|              | 🎯 Important milestones     | 🎯 Cleaning up history       |

**Golden Rules:**

- ✅ **Rebase:** Personal feature branches যেখানে শুধু আপনি কাজ করছেন
- ✅ **Merge:** Main/develop branches বা team-এর shared branches
- ❌ **Never rebase:** Public/shared branches (main, develop, release)

---

## ✅ Phase 3 চেকলিস্ট

Phase 3 complete করার জন্য:

- [ ] একই file/line-এ দুই branch-এ conflicting changes তৈরি করেছেন
- [ ] Merge conflict manually resolve করেছেন
- [ ] Merge abort করেছেন যখন conflict fix করতে চাননি
- [ ] Rebase conflict resolve করেছেন
- [ ] VS Code দিয়ে visually conflict resolve করেছেন
- [ ] Merge commit history-তে observe করেছেন
- [ ] Rebase করে linear history তৈরি করেছেন
- [ ] Merge vs Rebase-এর পার্থক্য বুঝেছেন
- [ ] অন্তত ৩টি আলাদা conflict scenario handle করেছেন ✨

---

## 📚 Conflict Resolution চিট শীট

### Conflict Detect করা

```bash
git status
# Unmerged paths:
#   both modified:   file.txt
```

### Merge Conflict Resolution

```bash
# 1. Conflict-ed file edit করুন
nano file.txt    # Markers মুছে content fix করুন

# 2. Resolved file stage করুন
git add file.txt

# 3. Merge complete করুন
git commit    # Message already prepared
```

### Rebase Conflict Resolution

```bash
# 1. Conflict-ed file edit করুন
nano file.txt

# 2. Resolved file stage করুন
git add file.txt

# 3. Rebase continue করুন
git rebase --continue

# যদি আরও conflicts থাকে, repeat
# সব resolved হলে rebase complete হবে
```

### Conflict Abort করা

```bash
# Merge abort
git merge --abort

# Rebase abort
git rebase --abort

# সব original state-এ ফিরে যাবে
```

### VS Code Shortcuts

```
Ctrl+Shift+G    → Source Control panel
Ctrl+K Ctrl+O   → Open folder
Ctrl+P          → Quick file open
Ctrl+Shift+P    → Command palette
```

---

## 💡 Pro Tips

### Tip 1: Conflict Prevention

```bash
# ঘন ঘন main থেকে pull করুন
git checkout main
git pull origin main
git checkout feature-branch
git merge main    # Regular sync

# Conflicts ছোট ছোট থাকবে, easier to fix
```

### Tip 2: Check Before Merge

```bash
# Merge করার আগে check করুন কী হবে
git merge --no-commit --no-ff feature-branch

# যদি conflict দেখেন, abort করুন
git merge --abort

# Ready হলে actual merge করুন
git merge feature-branch
```

### Tip 3: Conflict Markers সঠিকভাবে বুঝুন

```diff
<<<<<<< HEAD              ← Current branch (যেখানে আছেন)
Current branch content
=======                   ← Separator
Incoming branch content
>>>>>>> feature-branch    ← Branch যেটা merge হচ্ছে
```

### Tip 4: Git Mergetool ব্যবহার

```bash
# External merge tool configure করুন
git config --global merge.tool vscode
git config --global mergetool.vscode.cmd 'code --wait $MERGED'

# Conflict-এ mergetool চালান
git mergetool
```

---

## ⚠️ সাধারণ ভুল

### ভুল ১: Conflict Markers File-এ রেখে Commit

```bash
# ❌ ভুল - Markers সহ commit
git add file.txt    # Markers still there!
git commit -m "fixed"

# ✅ সঠিক - Markers মুছে তারপর
# Edit file, remove <<<, ===, >>>
git add file.txt
git commit -m "resolved conflict"
```

### ভুল ২: Shared Branch Rebase করা

```bash
# ❌ বিপজ্জনক - Main branch rebase
git checkout main
git rebase feature    # DON'T!

# ✅ সঠিক - Main branch merge
git checkout main
git merge feature
```

### ভুল ৩: Conflict Resolution না করেই Commit

```bash
# Conflict আছে কিনা check করুন
git status

# যদি "Unmerged paths" দেখেন
# Resolution করতে হবে!

# Resolution হয়েছে কিনা verify
git diff --check    # Should be empty
```

---

## 🎯 Phase 3 সম্পন্ন! এখন আপনি...

✅ Git conflicts বুঝেন এবং fix করতে পারেন  
✅ Merge এবং Rebase দুটোই comfortable  
✅ VS Code দিয়ে visual resolution করতে পারেন  
✅ Merge vs Rebase কখন কোনটা use করবেন জানেন  
✅ Complex team scenarios handle করতে ready

**পরবর্তী:** 👉 **Phase 4: Recovery & History Cleanup**

Phase 4-এ আপনি commit undo, amend, interactive rebase, cherry-pick, এবং history cleanup শিখবেন!

# Phase 4: Recovery & History Cleanup

## 🎯 লক্ষ্য

এই Phase-এ আপনি Git-এর "undo magic" শিখবেন: ভুল commits ঠিক করা, history cleanup, commit moving, এবং advanced recovery techniques।

---

## 📝 টাস্ক ১: Wrong Branch থেকে Commit Move করা

### ১. ভুল Branch-এ Commit করেছেন → সঠিক Branch-এ নিয়ে যাওয়া

**বাংলায়:** আপনি main branch-এ থাকা অবস্থায় feature-এর কাজ করে commit করে ফেলেছেন। এখন সেই commit feature branch-এ নিয়ে যেতে হবে।

**Scenario:**

```bash
# ভুলবশত main-এ কাজ করেছেন
git checkout main

echo "New feature code" > feature-code.js
git add feature-code.js
git commit -m "feat: add new feature"

# Oh no! এটা main-এ হওয়ার কথা ছিল না!
```

**Solution: সঠিক Branch-এ Move করা**

**Method 1: Cherry-pick ব্যবহার করে**

```bash
# ১. Commit SHA note করুন
git log --oneline -1
# abc123d feat: add new feature    <-- এই SHA copy করুন

# ২. Feature branch তৈরি/switch করুন
git checkout -b feature-new-code

# ৩. Commit cherry-pick করুন
git cherry-pick abc123d
# Done! Commit feature branch-এ copy হয়ে গেছে

# ৪. Main branch clean করুন
git checkout main
git reset --hard HEAD~1
# Main branch-এ আর ঐ commit নেই
```

**Method 2: Branch তৈরি then Reset**

```bash
# ১. বর্তমান position থেকে নতুন branch তৈরি
git branch feature-new-code

# ২. Main branch clean করুন
git reset --hard HEAD~1

# ৩. Feature branch-এ switch করুন
git checkout feature-new-code
# Commit এখন এখানে আছে!
```

**Verify করুন:**

```bash
# Main branch check
git checkout main
git log --oneline -3
# ঐ commit নেই

# Feature branch check
git checkout feature-new-code
git log --oneline -3
# ঐ commit আছে!
```

---

## 📝 টাস্ক ২: Last Commit Amend করা

### ২. সর্বশেষ Commit-এ Forgotten Change যোগ + Message Fix

**বাংলায়:** Commit করার পরে মনে হলো একটা file যোগ করা ভুলে গেছেন বা message ভুল লিখেছেন।

**Scenario 1: File ভুলে গেছেন**

```bash
# প্রথম commit
echo "Main code" > app.js
git add app.js
git commit -m "feat: add application code"

# ওহ! CSS file ভুলে গেছি!
echo "Styles" > app.css
```

**Fix: Amend করে যোগ করুন**

```bash
# Forgotten file add করুন
git add app.css

# Last commit-এ amend করুন
git commit --amend --no-edit
# --no-edit মানে message পরিবর্তন করবেন না
```

**Scenario 2: Message ভুল**

```bash
git commit -m "feat: add appp"    # Typo!
```

**Fix: Message ঠিক করুন**

```bash
git commit --amend -m "feat: add app code properly"
```

**Scenario 3: দুটোই (File + Message)**

```bash
# Forgotten file যোগ করুন
echo "Config" > config.js
git add config.js

# Amend করুন নতুন message সহ
git commit --amend -m "feat: add app with config and styles"
```

**Verify:**

```bash
git log --oneline -1
git show HEAD --stat
# দেখবেন সব files এক commit-এ আছে
```

⚠️ **সাবধান:** শুধুমাত্র **last commit** amend করা যায়। এবং যদি commit already push করে থাকেন, force push লাগবে।

---

## 📝 টাস্ক ৩: Last Commit Undo (Changes রেখে)

### ৩. শেষ Commit Undo কিন্তু Changes Staged রাখা

**বাংলায়:** Commit করার পরে মনে হলো আরও কাজ করা দরকার, তাই commit undo করতে চান কিন্তু changes রাখতে চান।

```bash
# Commit করেছেন
git add file1.txt file2.txt
git commit -m "feat: incomplete work"

# মনে হলো আরও কাজ দরকার
```

**Solution: Soft Reset**

```bash
# Last commit undo করুন, changes staged রাখুন
git reset --soft HEAD~1

# Check করুন
git status
# Changes to be committed (staged):
#   file1.txt
#   file2.txt

# আরও কাজ করুন
echo "More work" >> file1.txt
git add file1.txt

# নতুন commit
git commit -m "feat: complete work with additions"
```

**Reset Modes তুলনা:**

| Mode      | Commit Undo? | Staging Area?        | Working Directory?    |
| --------- | ------------ | -------------------- | --------------------- |
| `--soft`  | ✅ Yes       | ✅ Keeps (staged)    | ✅ Keeps (unchanged)  |
| `--mixed` | ✅ Yes       | ❌ Clears (unstaged) | ✅ Keeps (unchanged)  |
| `--hard`  | ✅ Yes       | ❌ Clears            | ❌ Discards (DANGER!) |

---

## 📝 টাস্ক ৪: Last Commit + Changes সম্পূর্ণ Discard

### ৪. শেষ Commit এবং সব Changes মুছে ফেলা

⚠️ **খুব সাবধান!** এটি করলে সব কিছু **চিরতরে মুছে যাবে**!

**Scenario:**

```bash
# ভুল কাজ করেছেন
echo "Bad code" > bad.js
git add bad.js
git commit -m "bad: terrible code"

# আরও ভুল কাজ
echo "More bad" >> bad.js

# সব মুছে ফেলতে চান!
```

**Solution: Hard Reset**

```bash
# Last commit + সব changes discard
git reset --hard HEAD~1

# সব চলে গেছে!
git status
# nothing to commit, working tree clean

git log --oneline -3
# Bad commit নেই
```

**Multiple Commits Undo:**

```bash
# Last 3 commits undo
git reset --hard HEAD~3

# Specific commit-এ যাওয়া
git reset --hard abc123d    # SHA দিয়ে
```

**Recovery (যদি ভুলে করে ফেলেন):**

```bash
# Reflog check করুন
git reflog

# Output:
# abc123d HEAD@{0}: reset: moving to HEAD~1
# def456g HEAD@{1}: commit: bad: terrible code

# Lost commit recover করুন
git reset --hard def456g
```

---

## 📝 টাস্ক ৫: Interactive Rebase দিয়ে Commits Squash

### ৫. ৫টি ছোট Commit → ২-৩টি Clean Commit

**বাংলায়:** অনেকগুলো ছোট ছোট "WIP" বা "fix typo" commits আছে যেগুলো একসাথে করে clean করতে চান।

**Scenario: Messy History**

```bash
# ৫টি ছোট commits তৈরি করুন
git checkout -b feature-cleanup

echo "Part 1" > code.js
git add code.js
git commit -m "WIP: start feature"

echo "Part 2" >> code.js
git add code.js
git commit -m "WIP: continue"

echo "Part 3" >> code.js
git add code.js
git commit -m "fix typo"

echo "Part 4" >> code.js
git add code.js
git commit -m "WIP: almost done"

echo "Part 5" >> code.js
git add code.js
git commit -m "final touches"

# History দেখুন
git log --oneline -5
```

**Solution: Interactive Rebase**

```bash
# শেষ ৫টি commit interactive rebase
git rebase -i HEAD~5
```

**Editor খুলবে:**

```bash
pick abc123d WIP: start feature
pick def456g WIP: continue
pick ghi789j fix typo
pick jkl012m WIP: almost done
pick mno345p final touches

# Commands:
# p, pick = use commit
# r, reword = use commit, but edit message
# s, squash = use commit, but meld into previous
# f, fixup = like squash, but discard commit message
# d, drop = remove commit
```

**Squash করুন (2টি commit-এ):**

```bash
pick abc123d WIP: start feature
squash def456g WIP: continue
squash ghi789j fix typo
pick jkl012m WIP: almost done
squash mno345p final touches

# Save এবং close editor
```

**Commit Message Editor আসবে:**

```bash
# প্রথম commit message
feat: implement core feature functionality

Added main code structure and logic for the feature.

# দ্বিতীয় commit message
feat: complete feature with final touches

Completed all edge cases and finalized implementation.
```

**Verify:**

```bash
git log --oneline -5
# abc123d feat: implement core feature functionality
# jkl012m feat: complete feature with final touches
```

**আরও উদাহরণ:**

**Scenario: একটিতে সব Squash**

```bash
git rebase -i HEAD~5

# Editor-এ:
pick abc123d First commit
fixup def456g Second (discard message)
fixup ghi789j Third (discard message)
fixup jkl012m Fourth (discard message)
fixup mno345p Fifth (discard message)

# Result: শুধু একটি commit!
```

---

## 📝 টাস্ক ৬: Cherry-pick - নির্দিষ্ট Commit Copy

### ৬. অন্য Branch থেকে একটি Commit নিয়ে আসা

**বাংলায়:** Feature branch-এ একটি specific commit আছে যেটা main-এ দরকার, কিন্তু পুরো branch merge করতে চান না।

**Scenario:**

```bash
# Feature branch-এ কাজ
git checkout -b feature-experiment

echo "Useful utility" > utils.js
git add utils.js
git commit -m "feat: add useful utility function"

echo "Experimental code" > experiment.js
git add experiment.js
git commit -m "experiment: trying new approach"

echo "More experiments" >> experiment.js
git add experiment.js
git commit -m "experiment: more trials"

# Commits:
# abc123d experiment: more trials
# def456g experiment: trying new approach
# ghi789j feat: add useful utility function    <-- এটা main-এ দরকার!
```

**Solution: Cherry-pick**

```bash
# Main branch-এ যান
git checkout main

# নির্দিষ্ট commit cherry-pick করুন
git cherry-pick ghi789j

# Done! utils.js এখন main-এ আছে
git log --oneline -1
# ghi789j feat: add useful utility function

git ls-files
# utils.js আছে, কিন্তু experiment.js নেই!
```

**Multiple Commits Cherry-pick:**

```bash
# একাধিক commits একসাথে
git cherry-pick abc123d def456g ghi789j

# Range cherry-pick
git cherry-pick abc123d^..ghi789j
```

**Conflict হলে:**

```bash
git cherry-pick abc123d
# CONFLICT!

# Resolve conflict
nano conflicted-file.txt

# Continue
git add conflicted-file.txt
git cherry-pick --continue

# অথবা abort
git cherry-pick --abort
```

---

## 📝 টাস্ক ৭: Reset --mixed (Working Directory রেখে)

### ৭. Commit Undo, Staging Clear, কিন্তু Files ঠিক রাখা

**বাংলায়:** Commit undo করতে চান এবং staging area clear করতে চান, কিন্তু আপনার করা changes ঠিক রাখতে চান (unstaged অবস্থায়)।

```bash
# কিছু commits করেছেন
echo "Code 1" > file1.js
git add file1.js
git commit -m "commit 1"

echo "Code 2" > file2.js
git add file2.js
git commit -m "commit 2"

echo "Code 3" > file3.js
git add file3.js
git commit -m "commit 3"
```

**Reset --mixed (Default)**

```bash
# Last 2 commits undo
git reset HEAD~2

# অথবা
git reset --mixed HEAD~2

# Check করুন
git status
# Changes not staged for commit:
#   file2.js
#   file3.js

git log --oneline -3
# Only commit 1 আছে

# Files আছে কিন্তু unstaged
ls
# file1.js  file2.js  file3.js
```

**Use Case:**

- Commits ভেঙে আলাদা করতে চান
- Commits reorder করতে চান
- Commit message পুরোপুরি পরিবর্তন করতে চান

---

## 📝 টাস্ক ৮: Reset --hard (সম্পূর্ণ Discard)

### ৮. Earlier Commit-এ যাওয়া এবং সব পরবর্তী কাজ Discard

⚠️ **অত্যন্ত বিপজ্জনক! সাবধান!**

**Scenario:**

```bash
# আপনার commits:
git log --oneline -5
# abc123d (HEAD) Bad work 3
# def456g Bad work 2
# ghi789j Bad work 1
# jkl012m Good commit
# mno345p Earlier commit
```

**Back to "Good commit":**

```bash
# "Good commit"-এ ফিরে যান, পরের সব মুছে ফেলুন
git reset --hard jkl012m

git log --oneline -3
# jkl012m (HEAD) Good commit
# mno345p Earlier commit

# Bad commits চলে গেছে!
# Files-ও চলে গেছে!
```

**Recovery (যদি ভুল করে ফেলেন):**

```bash
# Reflog দেখুন
git reflog
# abc123d HEAD@{1}: commit: Bad work 3
# def456g HEAD@{2}: commit: Bad work 2
# ghi789j HEAD@{3}: commit: Bad work 1
# jkl012m HEAD@{4}: reset: moving to jkl012m

# Recovery
git reset --hard abc123d
# সব ফিরে এলো!
```

---

## 🎯 চূড়ান্ত চ্যালেঞ্জ: Messy History Cleanup

### Goal: ৪+ "Bad" Commits → ১-২ "Good" Commits

**Start: Messy History**

```bash
git checkout -b cleanup-challenge

echo "a" > code.js && git add code.js && git commit -m "start"
echo "b" >> code.js && git add code.js && git commit -m "WIP"
echo "c" >> code.js && git add code.js && git commit -m "fix"
echo "d" >> code.js && git add code.js && git commit -m "typo"
echo "e" >> code.js && git add code.js && git commit -m "WIP again"
echo "f" >> code.js && git add code.js && git commit -m "done maybe"

git log --oneline -6
# Messy!
```

**Cleanup Strategy:**

```bash
# Interactive rebase
git rebase -i HEAD~6

# Squash করুন:
pick abc123d start
fixup def456g WIP
fixup ghi789j fix
pick jkl012m typo
fixup mno345p WIP again
fixup pqr678s done maybe

# Message update:
# Commit 1: feat: implement feature core
# Commit 2: style: fix code formatting

# Result:
git log --oneline -2
# Clean history! ✨
```

---

## ✅ Phase 4 চেকলিস্ট

- [ ] Wrong branch থেকে commit সঠিক branch-এ move করেছেন
- [ ] Last commit amend করেছেন (file + message)
- [ ] Commit undo করেছেন কিন্তু changes রেখেছেন (--soft)
- [ ] Commit + changes সম্পূর্ণ discard করেছেন (--hard)
- [ ] Interactive rebase করে ৫+ commits squash করেছেন
- [ ] Cherry-pick করে specific commit copy করেছেন
- [ ] --mixed reset করেছেন (unstaged রেখে)
- [ ] --hard reset করেছেন এবং reflog দিয়ে recover করেছেন
- [ ] ৪+ messy commits cleanup করে ১-২ clean commits তৈরি করেছেন ✨

---

## 📚 দ্রুত রেফারেন্স

### Reset Modes

```bash
git reset --soft HEAD~1    # Undo commit, keep staged
git reset --mixed HEAD~1   # Undo commit, unstage files
git reset --hard HEAD~1    # Undo commit, discard changes
```

### Commit Modification

```bash
git commit --amend                  # Edit last commit
git commit --amend --no-edit       # Add to last commit
git rebase -i HEAD~n               # Interactive rebase n commits
```

### Commit Moving

```bash
git cherry-pick abc123d            # Copy commit
git branch new-branch              # Create branch at current
git reset --hard HEAD~1            # Move current branch back
```

### Recovery

```bash
git reflog                         # See all ref changes
git reset --hard HEAD@{n}          # Recover to specific state
```

---

## 🎓 Phase 4 সম্পন্ন!

✅ Commit history cleanup করতে পারেন  
✅ Commits move, amend, squash করতে পারেন  
✅ Recovery techniques জানেন  
✅ Professional-quality history maintain করতে পারেন

**পরবর্তী:** 👉 **Phase 5: Team Simulation & GitHub Integration**

# Phase 5: Team Simulation & GitHub Integration

## 🎯 লক্ষ্য

এই Phase-এ আপনি real-world team collaboration, GitHub PR workflow, conflict resolution in PRs, এবং disaster recovery শিখবেন। Final capstone challenge-এ সব কিছু একসাথে practice করবেন।

---

## 📝 টাস্ক ১: Team Flow Simulation

### ১. দুই Feature Branch তৈরি করা (Team Member হিসেবে)

**বাংলায়:** আপনি একজন team member হিসেবে দুটি আলাদা feature নিয়ে কাজ করছেন।

**Feature 1: Login System**

```bash
# Feature 1 branch তৈরি
git checkout main
git pull origin main
git checkout -b feature/login-system

# Login feature implement
echo "Login Form" > login.html
echo "Login Logic" > login.js
git add login.html login.js
git commit -m "feat(auth): implement login system"

echo "Login Styles" > login.css
git add login.css
git commit -m "style(auth): add login styling"

# Push to remote
git push -u origin feature/login-system
```

**Feature 2: User Profile**

```bash
# Feature 2 branch তৈরি
git checkout main
git checkout -b feature/user-profile

# Profile feature implement
echo "Profile Page" > profile.html
echo "Profile Logic" > profile.js
git add profile.html profile.js
git commit -m "feat(user): implement user profile page"

echo "Profile Styles" > profile.css
git add profile.css
git commit -m "style(user): add profile styling"

# Push to remote
git push -u origin feature/user-profile
```

**Verify:**

```bash
# Local branches
git branch
# * feature/user-profile
#   feature/login-system
#   main

# Remote branches
git branch -r
# origin/feature/login-system
# origin/feature/user-profile
# origin/main
```

---

## 📝 টাস্ক ২: Team Lead হিসেবে Merge

### ২. দুই Feature Main-এ Merge করা (Command Line থেকে)

**বাংলায়:** এখন আপনি team lead হিসেবে দুটি feature review করে main-এ merge করবেন।

**Merge Feature 1:**

```bash
# Team lead হিসেবে main-এ যান
git checkout main
git pull origin main

# Feature 1 merge করুন
git merge feature/login-system

# Verify
git log --oneline -3
# abc123d Merge branch 'feature/login-system'
# def456g style(auth): add login styling
# ghi789j feat(auth): implement login system

# Remote-এ push করুন
git push origin main
```

**Merge Feature 2:**

```bash
# Feature 2 merge করুন
git merge feature/user-profile

# Verify
git log --oneline --graph -5

# Push to remote
git push origin main
```

**Cleanup (Merged Branches Delete):**

```bash
# Local branches delete
git branch -d feature/login-system
git branch -d feature/user-profile

# Remote branches delete
git push origin --delete feature/login-system
git push origin --delete feature/user-profile
```

---

## 📝 টাস্ক ৩: Developer - Update Local & Push New Work

### ৩. Leader Merge করার পর Local Update এবং নতুন কাজ

**বাংলায়:** Team lead merge করেছে। এখন আপনি developer হিসেবে আপনার local update করে নতুন কাজ করবেন।

**Update Local Main:**

```bash
# Main branch update
git checkout main
git pull origin main

# History check - leader এর merges দেখবেন
git log --oneline --graph -10
```

**New Feature Branch:**

```bash
# নতুন feature start
git checkout -b feature/dashboard

# Dashboard implement
echo "Dashboard Layout" > dashboard.html
git add dashboard.html
git commit -m "feat(dashboard): add dashboard layout"

echo "Dashboard Charts" > charts.js
git add charts.js
git commit -m "feat(dashboard): add interactive charts"

# Push করুন
git push -u origin feature/dashboard
```

**Main এ আবার update check:**

```bash
# অন্য developer আবার কিছু merge করেছে (simulate)
git checkout main
git pull origin main

# আপনার feature branch update
git checkout feature/dashboard
git merge main
# অথবা
git rebase main

# Push করুন
git push origin feature/dashboard
```

---

## 📝 টাস্ক ৪: GitHub Pull Request তৈরি ও Merge

### ৪. GitHub UI থেকে PR তৈরি এবং Merge করা

**বাংলায়:** Professional workflow-এ সরাসরি merge না করে Pull Request তৈরি করা হয়।

**Step 1: Feature Branch Push করুন**

```bash
# যদি আগে push না করে থাকেন
git checkout feature/dashboard
git push -u origin feature/dashboard
```

**Step 2: GitHub-এ PR তৈরি করুন**

1. **GitHub repository-তে যান**
2. **"Compare & pull request" বাটন দেখবেন** (হলুদ banner)
3. **Click করুন**

**PR Details পূরণ করুন:**

```markdown
Title: feat(dashboard): Add interactive dashboard with charts

Description:

## Changes

- Added dashboard layout HTML
- Implemented interactive charts using Chart.js
- Added responsive styling

## Testing

- Tested on Chrome, Firefox, Safari
- Mobile responsive verified
- All charts rendering correctly

## Screenshots

[Add screenshots if available]

## Related Issues

Closes #123
```

4. **"Create pull request" click করুন**

**Step 3: Review Process (Team Lead হিসেবে)**

**GitHub PR Page-এ:**

- **"Files changed" tab** - Code review করুন
- **Comments যোগ করুন** যদি কোনো suggestion থাকে
- **"Approve" করুন** যদি সব ঠিক থাকে

**Step 4: Merge করুন**

**Merge Options:**

1. **Create a merge commit** (Default)
   - Merge commit তৈরি হবে
   - History-তে branch relationship দেখাবে
2. **Squash and merge**
   - সব commits একটিতে combine হবে
   - Clean history
3. **Rebase and merge**
   - Linear history
   - No merge commit

```
[Merge pull request] বাটন click করুন
```

**Step 5: Local Cleanup**

```bash
# Main branch update
git checkout main
git pull origin main

# Feature branch delete
git branch -d feature/dashboard

# Verify
git log --oneline -5
# PR merge commit দেখবেন
```

---

## 📝 টাস্ক ৫: GitHub-এ Conflict Resolve করা

### ৫. Pull Request-এ Conflict আসলে GitHub UI থেকে Resolve

**বাংলায়:** PR তৈরির পর যদি main branch update হয় তাহলে conflict আসতে পারে।

**Scenario: Conflict তৈরি**

**Developer 1: Feature Branch**

```bash
git checkout -b feature/header
echo "Header Version 1" > header.html
git add header.html
git commit -m "feat: add header version 1"
git push -u origin feature/header
# PR তৈরি করেন কিন্তু merge করেননি
```

**Developer 2: Main-এ Same File Update**

```bash
git checkout main
echo "Header Version 2" > header.html
git add header.html
git commit -m "feat: add header version 2"
git push origin main
```

**এখন PR-এ Conflict দেখাবে!**

**GitHub-এ Conflict Resolve:**

1. **PR page-এ "Resolve conflicts" বাটন দেখবেন**
2. **Click করুন**

**GitHub Editor-এ Conflict:**

```html
<<<<<<< feature/header Header Version 1 ======= Header Version 2 >>>>>>> main
```

3. **Manually Edit করুন:**

```html
Header Version 1 and Version 2 combined
```

4. **"Mark as resolved" click করুন**
5. **"Commit merge" click করুন**
6. **"Merge pull request" করুন**

**Alternative: Terminal থেকে Resolve**

```bash
# Feature branch update করুন
git checkout feature/header
git pull origin main
# CONFLICT আসবে

# Resolve করুন
nano header.html
# Conflict fix করুন

# Commit করুন
git add header.html
git commit -m "Merge main into feature/header: resolve header conflict"

# Push করুন
git push origin feature/header

# এখন GitHub PR-এ conflict নেই!
# Merge করতে পারবেন
```

---

## 📝 টাস্ক ৬: Disaster Recovery

### ৬. Main Branch-এ Junk Commit → Reset দিয়ে Recovery

**বাংলায়:** ভুলবশত main branch-এ খারাপ code commit করে ফেলেছেন। এখন recover করতে হবে।

**Disaster: Junk Code Main-এ**

```bash
git checkout main

# ভুল করে junk commit
echo "DELETE THIS" > junk.js
echo "BAD CODE" > terrible.js
git add junk.js terrible.js
git commit -m "OOPS: accidental commit"
git push origin main

# আরেকটি bad commit
echo "MORE BAD" >> junk.js
git add junk.js
git commit -m "OOPS: more mistakes"
git push origin main
```

**Recovery Plan:**

**Step 1: খুঁজে বের করুন Good Commit**

```bash
# History দেখুন
git log --oneline -10

# Output:
# abc123d OOPS: more mistakes      <-- Bad
# def456g OOPS: accidental commit  <-- Bad
# ghi789j feat: good feature       <-- Good! এখানে ফিরতে হবে
```

**Step 2: Local Reset করুন**

```bash
# Good commit-এ reset করুন
git reset --hard ghi789j

# Verify
git log --oneline -3
# Bad commits নেই!
```

**Step 3: Force Push (সাবধানে!)**

```bash
# Force push করুন
git push --force origin main

# অথবা safer version
git push --force-with-lease origin main
```

⚠️ **Team-কে Inform করুন!**

```bash
# Team members-কে বলুন তারা যেন fresh clone করে নেয়
# অথবা:
git fetch origin
git reset --hard origin/main
```

**Alternative: Revert (Safer for Team)**

```bash
# Reset-এর বদলে revert করুন
git revert abc123d    # First bad commit revert
git revert def456g    # Second bad commit revert

# Push করুন (no force needed!)
git push origin main

# History preserved, সব undo হয়ে গেছে
```

---

## 📝 টাস্ক ৭: Full End-to-End Challenge

### ৭. সম্পূর্ণ Professional Workflow একবারে

**বাংলায়:** একটি পূর্ণাঙ্গ realistic scenario যেখানে সব কিছু একসাথে করবেন।

**Scenario: নতুন Feature Add করা**

**Step 1: Fresh Day Start**

```bash
# Morning routine
git checkout main
git pull origin main
git branch --merged    # Old branches clean করুন
```

**Step 2: Feature Branch তৈরি**

```bash
git checkout -b feature/payment-integration
```

**Step 3: Work & 3 Commits**

```bash
# Commit 1
echo "Payment Gateway Setup" > payment.js
git add payment.js
git commit -m "feat(payment): add payment gateway integration"

# Commit 2
echo "Payment Form UI" > payment-form.html
git add payment-form.html
git commit -m "feat(payment): create payment form UI"

# Commit 3
echo "Payment Validation" > payment-validation.js
git add payment-validation.js
git commit -m "feat(payment): add payment validation logic"
```

**Step 4: Main Update হয়েছে (Team এর কাজ)**

```bash
# Simulate: অন্য developer main-এ merge করেছে
git checkout main
echo "Team update" > team-file.js
git add team-file.js
git commit -m "feat: team member's feature"
git push origin main
```

**Step 5: Conflict Scenario**

```bash
# আপনার feature branch-এ same file edit করেছেন
git checkout feature/payment-integration
echo "Payment conflicts" > team-file.js
git add team-file.js
git commit -m "feat(payment): update team file"
```

**Step 6: Sync & Resolve Conflict**

```bash
# Main থেকে pull করুন
git checkout main
git pull origin main

# Feature branch-এ merge করুন
git checkout feature/payment-integration
git merge main
# CONFLICT in team-file.js!

# Resolve conflict
nano team-file.js
# Fix করুন

git add team-file.js
git commit -m "Merge main: resolve team-file conflict"
```

**Step 7: Clean History (Squash করুন)**

```bash
# Interactive rebase করে clean করুন
git rebase -i HEAD~4

# 4 commits → 2 commits
pick abc123d feat(payment): add payment gateway
squash def456g feat(payment): create payment form
pick ghi789j feat(payment): add payment validation
squash jkl012m Merge main
```

**Step 8: PR তৈরি করুন**

```bash
# Push করুন
git push -u origin feature/payment-integration

# GitHub-এ যান
# PR তৈরি করুন
```

**Step 9: Merge করুন**

```
GitHub-এ:
1. Review করুন
2. Approve করুন
3. Squash and Merge করুন
4. Delete branch করুন
```

**Step 10: Cleanup**

```bash
# Local main update
git checkout main
git pull origin main

# Feature branch delete
git branch -d feature/payment-integration

# Verify
git log --oneline -5
```

---

## 🎯 Final Challenge (Capstone)

### ৮. সম্পূর্ণ Cycle ১০ মিনিটে করুন!

**লক্ষ্য:** নিচের পুরো flow ১০ মিনিটের মধ্যে শেষ করুন (সময় গুনুন!)

**Timer Start! ⏱️**

**1. Sync (1 min)**

```bash
git checkout main && git pull origin main
```

**2. Feature Branch + 3 Commits (3 min)**

```bash
git checkout -b feature/fast-feature
echo "1" > f1.txt && git add f1.txt && git commit -m "feat: part 1"
echo "2" > f2.txt && git add f2.txt && git commit -m "feat: part 2"
echo "3" > f3.txt && git add f3.txt && git commit -m "feat: part 3"
```

**3. Simulate Conflict + Resolve (3 min)**

```bash
# Main update
git checkout main
echo "conflict" > shared.txt && git add shared.txt && git commit -m "main update"

# Feature update same file
git checkout feature/fast-feature
echo "feature" > shared.txt && git add shared.txt && git commit -m "feature update"

# Merge & resolve
git merge main
# Fix conflict in shared.txt
git add shared.txt && git commit -m "resolve conflict"
```

**4. Rebase Clean (2 min)**

```bash
git rebase -i HEAD~5
# Squash to 2 commits
```

**5. PR + Merge (1 min)**

```bash
git push -u origin feature/fast-feature
# GitHub-এ PR তৈরি + Merge করুন (30 sec)
git checkout main && git pull origin main
git branch -d feature/fast-feature
```

**Timer Stop! ⏱️**

**লক্ষ্য:**

- ✅ 10 মিনিটের কম = Expert level!
- ✅ 10-15 মিনিট = Good, practice আরো করুন
- ⚠️ 15+ মিনিট = আরও practice দরকার

**3 বার Practice করুন যতক্ষণ না 10 মিনিটের নিচে আসেন!**

---

## ✅ Phase 5 সম্পন্ন! Final Checklist

- [ ] দুই feature branch তৈরি করে কাজ করেছেন
- [ ] Team lead হিসেবে command line থেকে merge করেছেন
- [ ] Developer হিসেবে leader-এর merge পরে update করেছেন
- [ ] GitHub-এ PR তৈরি করেছেন এবং web interface থেকে merge করেছেন
- [ ] GitHub PR-এ conflict resolve করেছেন
- [ ] Main branch-এ disaster simulate করে recover করেছেন
- [ ] Full end-to-end realistic workflow complete করেছেন
- [ ] Capstone challenge ১০ মিনিটে complete করেছেন ⚡

---

## 📚 GitHub PR Best Practices

### PR Title Conventions

```
feat(scope): add new feature
fix(scope): resolve bug
docs(scope): update documentation
style(scope): formatting changes
refactor(scope): code restructure
test(scope): add tests
chore(scope): maintenance tasks
```

### PR Description Template

```markdown
## Summary

[Brief description of changes]

## Changes

- Change 1
- Change 2
- Change 3

## Testing

- [ ] Unit tests pass
- [ ] Manual testing done
- [ ] Edge cases covered

## Screenshots

[If applicable]

## Related Issues

Closes #123
Relates to #456
```

### Review Checklist

```markdown
- [ ] Code follows project conventions
- [ ] No console.logs or debug code
- [ ] Tests added/updated
- [ ] Documentation updated
- [ ] No merge conflicts
- [ ] CI/CD passing
```

---

## 💡 Pro Tips

### Tip 1: PR Size

```
✅ Good: 200-400 lines changed
⚠️ OK: 400-800 lines
❌ Too large: 800+ lines (split করুন!)
```

### Tip 2: Draft PRs

```bash
# কাজ চলছে but feedback চান?
# GitHub-এ "Draft PR" তৈরি করুন
```

### Tip 3: PR Templates

```bash
# .github/pull_request_template.md তৈরি করুন
# Automatic template load হবে
```

### Tip 4: Force Push সাবধানে

```bash
# ❌ বিপজ্জনক
git push --force

# ✅ নিরাপদ (remote change থাকলে fail করবে)
git push --force-with-lease
```

---

## 🏆 সব Phase সম্পন্ন! অভিনন্দন!

আপনি এখন জানেন:

✅ **Phase 1:** Git basics, config, staging, commits  
✅ **Phase 2:** Branching, daily workflow, sync  
✅ **Phase 3:** Conflict resolution, merge vs rebase  
✅ **Phase 4:** History cleanup, recovery, advanced commands  
✅ **Phase 5:** Team collaboration, GitHub PRs, professional workflow

## 🎯 পরবর্তী পদক্ষেপ

1. **Real Project-এ Practice করুন**
   - একটি personal project শুরু করুন
   - GitHub-এ public repo রাখুন
   - Regular commits করুন

2. **Open Source Contribute করুন**
   - GitHub-এ interesting projects খুঁজুন
   - Issues solve করুন
   - PR submit করুন

3. **Advanced Topics শিখুন**
   - Git hooks
   - Git submodules
   - Git LFS (Large File Storage)
   - CI/CD integration

4. **Team-এ Join করুন**
   - Real collaboration experience
   - Code review করুন
   - Professional workflows follow করুন

---

## 📖 Additional Resources

### Useful Git Aliases

```bash
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.cm commit
git config --global alias.pl pull
git config --global alias.ps push
git config --global alias.lg "log --oneline --graph --all"
git config --global alias.undo "reset --soft HEAD~1"
```

### Git Cheat Sheet Link

- [GitHub Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)
- [Atlassian Git Tutorials](https://www.atlassian.com/git/tutorials)
- [Git Documentation](https://git-scm.com/doc)

---

**🎓 আপনি এখন Git Professional! এগিয়ে যান এবং amazing projects তৈরি করুন! 🚀**

## 📚 Quick Reference

### সবচেয়ে ব্যবহৃত Commands

```bash
# Daily Workflow
git status                    # Check status
git add .                     # Stage all
git commit -m "message"       # Commit
git push origin branch        # Push
git pull origin main          # Pull

# Branching
git checkout -b branch-name   # Create + switch
git branch                    # List branches
git branch -d branch-name     # Delete branch
git merge branch-name         # Merge branch

# History
git log --oneline            # Compact history
git log --graph --all        # Visual history
git reflog                   # See all actions

# Undo
git reset --soft HEAD~1      # Undo commit, keep staged
git reset --hard HEAD~1      # Undo commit, discard all
git revert commit-sha        # Safe undo (creates new commit)

# Conflict Resolution
git merge --abort            # Abort merge
git rebase --abort           # Abort rebase
git diff                     # See changes
```

---

## 🛠️ Setup Checklist

### প্রথমবার Git Setup করার সময়:

```bash
# 1. Git install check
git --version

# 2. Global config
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 3. Default branch name
git config --global init.defaultBranch main

# 4. Useful aliases
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.lg "log --oneline --graph --all"

# 5. Editor setup (optional)
git config --global core.editor "code --wait"

# 6. Verify
git config --list
```

### GitHub Setup:

```bash
# 1. SSH key generate (recommended)
ssh-keygen -t ed25519 -C "your.email@example.com"

# 2. SSH key add to GitHub
# Copy key: cat ~/.ssh/id_ed25519.pub
# GitHub → Settings → SSH Keys → Add

# 3. Test connection
ssh -T git@github.com

# 4. First repo
git init
git add .
git commit -m "Initial commit"
git remote add origin git@github.com:username/repo.git
git push -u origin main
```

---

## 💡 Pro Tips Collection

### Tip 1: Commit Messages

```bash
# Good commit messages:
feat(auth): add login functionality
fix(api): resolve timeout issue
docs(readme): update installation steps
style(css): improve button styling
refactor(utils): optimize helper functions
```

### Tip 2: .gitignore Template

```bash
# Node.js
node_modules/
npm-debug.log
.env

# Python
__pycache__/
*.pyc
venv/

# General
.DS_Store
*.log
*.tmp
```

### Tip 3: Branch Naming

```bash
# Good names:
feature/user-authentication
bugfix/login-timeout
hotfix/security-patch
release/v1.2.0

# Bad names:
test
new-branch
fix
my-work
```

---

## 🔗 External Resources

### Official Documentation

- [Git Official Docs](https://git-scm.com/doc)
- [GitHub Docs](https://docs.github.com)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

### Interactive Learning

- [Learn Git Branching](https://learngitbranching.js.org/)
- [GitHub Skills](https://skills.github.com/)
- [Git Immersion](https://gitimmersion.com/)

### Video Tutorials

- [Git & GitHub Crash Course](https://www.youtube.com/results?search_query=git+github+bangla+tutorial)
- [Advanced Git](https://www.youtube.com/results?search_query=advanced+git+tutorial)

---

## 🆘 Common Problems & Solutions

### Problem 1: "Permission denied (publickey)"

```bash
# Solution: SSH key setup করুন
ssh-keygen -t ed25519 -C "your.email@example.com"
# Key GitHub-এ add করুন
```

### Problem 2: "fatal: not a git repository"

```bash
# Solution: Repository initialize করুন
git init
```

### Problem 3: "Your branch is behind"

```bash
# Solution: Pull করুন
git pull origin main
```

### Problem 4: "CONFLICT (content)"

```bash
# Solution: Conflict resolve করুন
# File edit করুন, markers মুছুন
git add resolved-file
git commit -m "Resolve conflict"
```

### Problem 5: "rejected: non-fast-forward"

```bash
# Solution: Pull তারপর push
git pull origin main
git push origin main
```

---

## 🎓 Certification & Next Steps

### আপনি সফলভাবে Complete করেছেন!

**Skills Acquired:**

- ✅ Git fundamentals
- ✅ Branching strategies
- ✅ Conflict resolution
- ✅ History management
- ✅ Team collaboration
- ✅ GitHub workflow
- ✅ Professional practices

**Next Level:**

1. **Open Source Contribution**
   - Good first issues খুঁজুন
   - PRs submit করুন
   - Community-তে active থাকুন

2. **Advanced Topics**
   - Git hooks automation
   - Git submodules
   - Git LFS (Large File Storage)
   - CI/CD integration

3. **Team Leadership**
   - Code review করুন
   - Git workflow design করুন
   - Team members-কে train করুন

---

## 🏆 Final Challenge

**30 Day Git Challenge:**

প্রতিদিন:

- 📅 কমপক্ষে ১টি meaningful commit
- 📅 ১টি নতুন Git command/feature try করুন
- 📅 GitHub streak maintain করুন

**30 দিন পরে আপনি হবেন:**

- ✅ Git expert
- ✅ 90+ GitHub commits
- ✅ Clean contribution graph
- ✅ Professional portfolio

---

## 📞 Support & Community

**যদি আটকে যান:**

1. Documentation পড়ুন
2. StackOverflow search করুন
3. GitHub Community forum-এ জিজ্ঞাসা করুন
4. Practice করতে থাকুন!

**মনে রাখবেন:**

> "The best way to learn Git is to make mistakes and learn how to fix them!"

---

## 🙏 Acknowledgments

এই গাইড তৈরি করা হয়েছে:

- ✨ বাংলা ভাষায় Git শিক্ষা সহজ করতে
- 💪 Hands-on practice-এর উপর focus করে
- 🎯 Real-world scenarios দিয়ে
- ❤️ Community-র উন্নতির জন্য

---

**🚀 এখন Practice শুরু করুন! আপনার Git Journey শুভ হোক!**

---

## 📄 Files Structure

```
d:/coding/L 4/Intern/
├── README_Git_Master.md          # এই file (Index)
├── Phase1_Complete.md            # Setup & Basics
├── Phase2_Complete.md            # Branching & Workflow
├── Phase3_Complete.md            # Conflict Resolution
├── Phase4_Complete.md            # Recovery & Cleanup
└── Phase5_Complete.md            # Team & GitHub
```

**সব files এক জায়গায়! শুরু করুন Phase 1 থেকে! 🎯**

# Cherry-Pick এবং Rebase - সম্পূর্ণ বাংলা গাইড

## 🎯 সূচিপত্র

1. [Cherry-Pick কী এবং কেন?](#cherry-pick)
2. [Rebase কী এবং কেন?](#rebase)
3. [Merge vs Rebase পার্থক্য](#merge-vs-rebase)
4. [Interactive Rebase](#interactive-rebase)
5. [Cherry-Pick vs Rebase - কখন কোনটা?](#comparison)
6. [Best Practices এবং Golden Rules](#best-practices)
7. [Hands-on Practice Exercises](#practice)

---

## 🍒 Cherry-Pick কী?

**সহজ বাংলায়:** অন্য branch থেকে **নির্দিষ্ট একটি বা কয়েকটি commit** বেছে নিয়ে আপনার বর্তমান branch-এ কপি করা। যেমন গাছ থেকে পছন্দের চেরি তুলে নেওয়া!

### 🤔 কেন Cherry-Pick করবেন?

**Scenario 1: Useful Commit কিন্তু পুরো Branch না**

```bash
# আপনার feature branch-এ একটি bug fix করেছেন
# কিন্তু পুরো feature ready না, শুধু bug fix main-এ দরকার

Feature Branch:
- abc123d fix: critical security bug  ← এটা দরকার!
- def456g feat: half-done new feature  ← এটা ready না

# Solution: শুধু bug fix cherry-pick করুন!
```

**Scenario 2: Wrong Branch-এ Commit**

```bash
# ভুলে main-এ commit করে ফেলেছেন
# সেটা feature branch-এ নিয়ে যেতে হবে
```

**Scenario 3: Hotfix Multiple Branches-এ**

```bash
# Production-এ bug পেয়েছেন
# Fix main, develop, release সব branches-এ দরকার
```

### 📝 Cherry-Pick কিভাবে করবেন?

**Basic Cherry-Pick:**

```bash
# Step 1: যে commit চান তার SHA খুঁজুন
git log --oneline feature-branch
# Output:
# abc123d fix: resolve critical bug  ← এটা চাই!
# def456g feat: new feature work
# ghi789j refactor: code cleanup

# Step 2: যে branch-এ নিতে চান সেখানে যান
git checkout main

# Step 3: Cherry-pick করুন
git cherry-pick abc123d

# Done! শুধু bug fix এসেছে
```

**একাধিক Commits Cherry-Pick:**

```bash
# Method 1: একসাথে 3টি commits
git cherry-pick abc123d def456g ghi789j

# Method 2: Range cherry-pick
git cherry-pick abc123d^..ghi789j
# abc123d থেকে ghi789j পর্যন্ত সব commits
```

### ⚠️ Cherry-Pick Conflict হলে কী করবেন?

```bash
# Cherry-pick করার সময় conflict
git cherry-pick abc123d

# Output:
# error: could not apply abc123d... fix bug
# hint: after resolving the conflicts, mark them with
# hint: 'git add <paths>' and run 'git cherry-pick --continue'

# Step 1: Conflict resolve করুন
nano conflicted-file.txt
# Markers (<<<, ===, >>>) মুছে fix করুন

# Step 2: Resolved file add করুন
git add conflicted-file.txt

# Step 3: Continue করুন
git cherry-pick --continue

# অথবা বাতিল করতে চাইলে
git cherry-pick --abort
```

### 💡 Cherry-Pick Tips

```bash
# Tip 1: Commit message edit করে cherry-pick
git cherry-pick abc123d --edit

# Tip 2: Cherry-pick কিন্তু commit করবেন না (পরে করবেন)
git cherry-pick abc123d --no-commit

# Tip 3: Author information রাখুন
git cherry-pick abc123d -x
# Commit message-এ যোগ হবে: "(cherry picked from commit abc123d)"

# Tip 4: Multiple commits এর মধ্যে skip করতে
git cherry-pick --skip
```

---

## 🔄 Rebase কী?

**সহজ বাংলায়:** আপনার branch-এর commits গুলো তুলে নিয়ে অন্য একটি branch-এর **উপরে নতুন করে বসানো**। এতে commit history **সোজা লাইনে (linear)** হয়।

### 🎨 Visual তুলনা: Merge vs Rebase

**Merge (Branch থাকে):**

```
Before Merge:
      C---D---E  (feature)
     /
A---B           (main)

After Merge:
      C---D---E  (feature)
     /         \
A---B-----------F  (main)
                ↑ Merge commit

History: A → B → C → D → E → F (branching দেখায়)
```

**Rebase (Linear হয়):**

```
Before Rebase:
      C---D---E  (feature)
     /
A---B---F---G   (main)

After Rebase:
A---B---F---G---C'---D'---E'  (feature)
                ↑
              main এর উপরে বসানো

History: A → B → F → G → C' → D' → E' (straight line)
```

### 🤔 কেন Rebase করবেন?

**কারণ 1: Clean History**

```bash
# Merge করলে:
* Merge branch 'feature' into main
* feat: add feature
* fix: typo
* Merge branch 'main' into feature
* feat: another feature
* Merge branch 'feature' into main
# অনেক merge commits, messy!

# Rebase করলে:
* feat: another feature
* feat: add feature
# Clean, readable!
```

**কারণ 2: Feature Branch Update**

```bash
# Main branch update হয়েছে, আপনার feature outdated
# Merge করলে: অনেক merge commits
# Rebase করলে: feature main-এর উপরে বসে যাবে, clean!
```

### 📝 Rebase কিভাবে করবেন?

**Basic Rebase:**

```bash
# Scenario: Feature branch main থেকে outdated
# Main-এ নতুন commits এসেছে

# Step 1: Feature branch-এ যান
git checkout feature-branch

# Step 2: Main-এর উপরে rebase করুন
git rebase main

# Done! Feature branch এখন main-এর উপরে
```

**Rebase with Update:**

```bash
# পুরো workflow (Daily sync)
# Step 1: Main update করুন
git checkout main
git pull origin main

# Step 2: Feature branch rebase করুন
git checkout feature-branch
git rebase main

# Step 3: যদি conflict না থাকে, push করুন
git push origin feature-branch

# যদি already push করা থাকে, force push লাগবে
git push --force-with-lease origin feature-branch
```

### ⚠️ Rebase Conflict হলে কী করবেন?

```bash
# Rebase করার সময় conflict
git rebase main

# Output:
# CONFLICT (content): Merge conflict in file.txt
# error: could not apply abc123d... your commit message

# Step 1: Conflict resolve করুন
git status  # Conflicted files দেখুন
nano file.txt  # Conflict fix করুন

# Step 2: Resolved files add করুন
git add file.txt

# Step 3: Rebase continue করুন
git rebase --continue

# যদি আরও conflicts থাকে, steps repeat করুন

# Rebase বাতিল করতে চাইলে
git rebase --abort
```

### 🚨 Rebase করার পর Push করা

```bash
# যদি আগে কখনো push না করে থাকেন
git push origin feature-branch

# যদি আগে push করা থাকে (rebase history change করেছে)
git push --force-with-lease origin feature-branch

# ⚠️ সাবধান!
# --force শুধু নিজের branch-এ ব্যবহার করুন
# Shared branch-এ NEVER!
```

---

## 🔀 Merge vs Rebase - বিস্তারিত তুলনা

| Feature              | Merge                         | Rebase                        |
| -------------------- | ----------------------------- | ----------------------------- |
| **History**          | Branching (merge commits আছে) | Linear (straight line)        |
| **Merge Commit**     | ✅ তৈরি হয়                   | ❌ হয় না (fast-forward)      |
| **Original Commits** | ✅ Preserve করে               | ❌ Re-write করে (নতুন SHA)    |
| **Readability**      | 🔴 Complex graph              | 🟢 Simple, easy to follow     |
| **Safety**           | 🟢 নিরাপদ (history না বদলায়) | 🔴 Dangerous (history বদলায়) |
| **Use Case**         | Main/shared branches          | Personal feature branches     |
| **Team Work**        | 🟢 Safe for shared work       | 🔴 Risky if others use branch |

### কখন Merge করবেন?

✅ **Merge Use Cases:**

```bash
# 1. Main/Master branches
git checkout main
git merge feature-branch

# 2. Team-এর shared branches
git checkout develop
git merge feature-team

# 3. Important milestones track করতে
git merge release-v1.0

# 4. Branch relationship দেখাতে চান
git merge feature-a feature-b
```

### কখন Rebase করবেন?

✅ **Rebase Use Cases:**

```bash
# 1. Personal feature branch update
git checkout my-feature
git rebase main

# 2. PR তৈরির আগে history clean করা
git rebase -i HEAD~5

# 3. Local commits organize করা
git rebase -i main

# 4. Feature branch-এ main-এর latest changes নেওয়া
git rebase main
```

### কখন Rebase করবেন না? 🚫

❌ **NEVER Rebase:**

```bash
# 1. Public/Shared branches
git checkout main
git rebase feature  # ❌ DON'T!

# 2. অন্যরা ব্যবহার করছে এমন branch
git checkout team-feature
git rebase main  # ❌ Team-এর problem হবে!

# 3. Already pushed এবং others pulled করেছে
git rebase main  # ❌ Others-এর সমস্যা!
```

---

## 🎯 Interactive Rebase - সবচেয়ে Powerful!

**বাংলায়:** একাধিক commits একসাথে edit করা - squash, reword, reorder, delete, edit!

### 📝 Interactive Rebase Commands

```bash
# শেষ 5টি commits edit করতে
git rebase -i HEAD~5

# Specific commit থেকে শুরু করতে
git rebase -i abc123d
```

**Editor খুলবে এরকম:**

```bash
pick abc123d First commit
pick def456g Second commit
pick ghi789j Third commit
pick jkl012m Fourth commit
pick mno345p Fifth commit

# Commands:
# p, pick   = commit ব্যবহার করুন (no change)
# r, reword = commit ব্যবহার করুন কিন্তু message edit করুন
# e, edit   = commit edit করুন (code change করতে পারবেন)
# s, squash = commit আগেরটার সাথে মিশিয়ে দিন (message রাখুন)
# f, fixup  = commit আগেরটার সাথে মিশিয়ে দিন (message ফেলে দিন)
# d, drop   = commit সম্পূর্ণ মুছে ফেলুন
# x, exec   = shell command run করুন
```

### 🔧 Interactive Rebase Use Cases

**Use Case 1: Squash (একসাথে মিশানো)**

```bash
# Before: 5টি messy commits
abc123d WIP: start feature
def456g WIP: continue work
ghi789j fix typo
jkl012m WIP: almost done
mno345p final touches

# Interactive rebase
git rebase -i HEAD~5

# Editor-এ পরিবর্তন করুন:
pick abc123d WIP: start feature
fixup def456g WIP: continue work
fixup ghi789j fix typo
pick jkl012m WIP: almost done
fixup mno345p final touches

# Save and close

# After: শুধু 2টি clean commits!
abc123d feat: implement core feature functionality
jkl012m feat: complete feature with polish
```

**Use Case 2: Reword (Message পরিবর্তন)**

```bash
# Before:
abc123d fix bug  # খারাপ message

# Interactive rebase
git rebase -i HEAD~1

# Editor-এ:
reword abc123d fix bug

# Save করলে নতুন editor আসবে message change করতে
# Change to: "fix: resolve login timeout issue"

# After:
abc123d fix: resolve login timeout issue  # ভালো message!
```

**Use Case 3: Reorder (Commits এর ক্রম পরিবর্তন)**

```bash
# Before:
abc123d feat: add feature A
def456g docs: update README
ghi789j feat: add feature B

# Interactive rebase
git rebase -i HEAD~3

# Editor-এ order পরিবর্তন করুন:
pick abc123d feat: add feature A
pick ghi789j feat: add feature B  # উপরে নিলাম
pick def456g docs: update README  # নিচে নিলাম

# After: Features একসাথে, docs শেষে
```

**Use Case 4: Edit (Commit-এ code পরিবর্তন)**

```bash
# একটি পুরানো commit-এ code যোগ করতে চান

git rebase -i HEAD~3

# Editor-এ:
edit abc123d feat: add feature
pick def456g fix: bug fix
pick ghi789j docs: update

# Save করলে rebase stop করবে abc123d-তে

# এখন code edit করুন
echo "forgotten code" >> file.txt
git add file.txt
git commit --amend --no-edit

# Continue করুন
git rebase --continue
```

**Use Case 5: Drop (Commit মুছে ফেলা)**

```bash
# Before:
abc123d feat: good feature
def456g debug: console.log added  ← মুছে ফেলতে চাই
ghi789j feat: another feature

# Interactive rebase
git rebase -i HEAD~3

# Editor-এ:
pick abc123d feat: good feature
drop def456g debug: console.log added  # অথবা এই line-ই মুছে ফেলুন
pick ghi789j feat: another feature

# After: debug commit চলে গেছে!
```

---

## 🎯 Cherry-Pick vs Rebase - কখন কোনটা?

### Decision Tree

```
আপনার সমস্যা কী?
│
├─ একটি/কয়েকটি specific commit copy করতে চাই
│  └─ 🍒 Cherry-Pick ব্যবহার করুন
│
├─ পুরো branch-এর history clean করতে চাই
│  └─ 🔄 Rebase ব্যবহার করুন
│
├─ Feature branch main-এর সাথে update করতে চাই
│  ├─ Personal branch? → 🔄 Rebase
│  └─ Shared branch? → 🔀 Merge
│
├─ অনেকগুলো commits একসাথে করতে চাই
│  └─ 🔄 Interactive Rebase (squash)
│
└─ Bug fix multiple branches-এ দরকার
   └─ 🍒 Cherry-Pick করুন প্রতিটিতে
```

### বাস্তব Scenarios

| Scenario                        | Solution                | Command                   |
| ------------------------------- | ----------------------- | ------------------------- |
| Wrong branch-এ commit করেছি     | 🍒 Cherry-Pick          | `git cherry-pick abc123d` |
| Feature branch outdated         | 🔄 Rebase               | `git rebase main`         |
| Hotfix সব branches-এ দরকার      | 🍒 Cherry-Pick          | `git cherry-pick fix-sha` |
| 10টি WIP commits cleanup        | 🔄 Interactive Rebase   | `git rebase -i HEAD~10`   |
| Main-এর নতুন code feature-এ চাই | 🔀 Merge অথবা 🔄 Rebase | নিজের branch? Rebase      |
| Team branch update              | 🔀 Merge (নিরাপদ)       | `git merge main`          |

---

## 💡 Best Practices এবং Golden Rules

### Cherry-Pick Best Practices

**✅ DO:**

```bash
# Specific commits যখন দরকার
git cherry-pick abc123d

# Bug fix duplicate করতে
git cherry-pick security-fix-sha

# Commit message clarify করতে
git cherry-pick abc123d -x
# Message-এ add হয়: "(cherry picked from commit abc123d)"
```

**❌ DON'T:**

```bash
# পুরো branch cherry-pick করবেন না
# এর জন্য merge/rebase আছে

# অতিরিক্ত cherry-pick
# History duplicate এবং confusing হয়
```

### Rebase Best Practices

**✅ DO:**

```bash
# Personal feature branch-এ
git checkout my-feature
git rebase main

# PR তৈরির আগে cleanup
git rebase -i HEAD~5

# Commit message improve
git rebase -i HEAD~3
# reword ব্যবহার করুন
```

**❌ DON'T:**

```bash
# Public/shared branch rebase করবেন না
git checkout main
git rebase feature  # ❌ NEVER!

# অন্যরা ব্যবহার করছে এমন branch
git checkout team-feature
git rebase main  # ❌ Team সমস্যায় পড়বে!

# Push করার পরে rebase
# যদি অন্যরা pull করে থাকে
```

---

## 🚨 Golden Rules

### Rule 1: Public History কখনো Rewrite করবেন না

```bash
# ❌ WRONG - Main branch rebase
git checkout main
git rebase develop

# ✅ RIGHT - Main branch merge
git checkout main
git merge develop
```

### Rule 2: নিজের Branch = Rebase OK, অন্যের Branch = Merge

```bash
# ✅ আপনার personal branch
git checkout my-feature
git rebase main  # OK!

# ❌ Team-এর shared branch
git checkout team-feature
git rebase main  # DON'T!
```

### Rule 3: Cherry-Pick সংযতভাবে ব্যবহার করুন

```bash
# ✅ Good use
git cherry-pick critical-hotfix

# ❌ Bad use - অনেকগুলো commits
git cherry-pick commit1 commit2 commit3 ... commit20
# এর জন্য merge/rebase ভালো
```

### Rule 4: Force Push করার আগে দুইবার চিন্তা করুন

```bash
# ❌ Dangerous
git push --force origin main

# ✅ Safer (যদি remote change থাকে, fail করবে)
git push --force-with-lease origin my-feature

# ✅ Best - নিজের branch-এই শুধু
```

---

## 🏋️ Hands-on Practice Exercises

### Exercise 1: Cherry-Pick Practice

**Setup:**

```bash
# 1. নতুন repo তৈরি
mkdir cherry-pick-practice && cd cherry-pick-practice
git init

# 2. Main branch-এ initial commit
echo "Main file" > main.txt
git add main.txt
git commit -m "initial: create main.txt"

# 3. Branch A তৈরি করুন
git checkout -b branch-a
echo "Feature A" > feature-a.txt
git add feature-a.txt
git commit -m "feat: add feature A"

echo "Bug fix for A" >> feature-a.txt
git add feature-a.txt
git commit -m "fix: resolve bug in feature A"

# 4. Main-এ ফিরে Branch B তৈরি করুন
git checkout main
git checkout -b branch-b
echo "Feature B" > feature-b.txt
git add feature-b.txt
git commit -m "feat: add feature B"
```

**Task:**

```bash
# Branch A-র bug fix Branch B-তে cherry-pick করুন
# (branch-a-র 2nd commit branch-b-তে নিন)

# Solution:
git log branch-a --oneline  # Bug fix SHA copy করুন
# abc123d fix: resolve bug in feature A

git checkout branch-b
git cherry-pick abc123d

# Verify
git log --oneline
```

### Exercise 2: Basic Rebase Practice

**Setup:**

```bash
# 1. নতুন repo
mkdir rebase-practice && cd rebase-practice
git init

# 2. Main branch commits
echo "Version 1" > app.txt
git add app.txt && git commit -m "v1"

echo "Version 2" >> app.txt
git add app.txt && git commit -m "v2"

# 3. Feature branch তৈরি করুন (v1 থেকে)
git checkout HEAD~1  # Go back to v1
git checkout -b feature
echo "Feature work 1" > feature.txt
git add feature.txt && git commit -m "feat: work 1"

echo "Feature work 2" >> feature.txt
git add feature.txt && git commit -m "feat: work 2"
```

**Task:**

```bash
# Feature branch main-এর উপরে rebase করুন

# Solution:
git checkout feature
git rebase main

# Verify - history linear হয়েছে কি না
git log --oneline --graph --all
```

### Exercise 3: Interactive Rebase - Squash Practice

**Setup:**

```bash
# 1. নতুন repo
mkdir squash-practice && cd squash-practice
git init

# 2. অনেকগুলো messy commits তৈরি করুন
echo "Start" > code.js
git add code.js && git commit -m "WIP: start"

echo "More code" >> code.js
git add code.js && git commit -m "WIP"

echo "Fix typo" >> code.js
git add code.js && git commit -m "fix typo"

echo "More work" >> code.js
git add code.js && git commit -m "WIP continue"

echo "Done" >> code.js
git add code.js && git commit -m "done"

# History messy!
git log --oneline
```

**Task:**

```bash
# 5টি commits → 1টি clean commit-এ squash করুন

# Solution:
git rebase -i HEAD~5

# Editor-এ:
pick abc123d WIP: start
fixup def456g WIP
fixup ghi789j fix typo
fixup jkl012m WIP continue
fixup mno345p done

# Message editor-এ:
# feat: implement complete feature

# Verify
git log --oneline  # শুধু 1টি commit!
```

### Exercise 4: Rebase Conflict Resolution

**Setup:**

```bash
# 1. Conflict scenario তৈরি করুন
mkdir conflict-rebase && cd conflict-rebase
git init

# 2. Main branch
echo "Version 1" > shared.txt
git add shared.txt && git commit -m "v1"

# 3. Feature branch (পুরানো base থেকে)
git checkout -b feature
echo "Feature version" > shared.txt
git add shared.txt && git commit -m "feat: feature version"

# 4. Main update (conflict তৈরি করবে)
git checkout main
echo "Main version" > shared.txt
git add shared.txt && git commit -m "main: main version"
```

**Task:**

```bash
# Feature branch rebase করুন এবং conflict resolve করুন

# Solution:
git checkout feature
git rebase main
# CONFLICT!

# Resolve conflict
nano shared.txt
# Choose করুন কোনটা রাখবেন অথবা দুটোই

git add shared.txt
git rebase --continue

# Verify
git log --oneline --graph --all
```

### Exercise 5: Real-World Workflow Simulation

**Scenario:** আপনি একটি feature নিয়ে কাজ করছেন। Main branch update হয়েছে। আপনার history messy। PR তৈরির আগে সব ঠিক করুন।

```bash
# Setup
mkdir real-workflow && cd real-workflow
git init

# Main branch
echo "App v1" > app.js
git add app.js && git commit -m "v1"

# Feature branch start (messy commits)
git checkout -b feature-user-auth
echo "Login" > login.js
git add login.js && git commit -m "WIP login"

echo "More login" >> login.js
git add login.js && git commit -m "continue"

echo "Signup" > signup.js
git add signup.js && git commit -m "WIP signup"

echo "Fix bug" >> login.js
git add login.js && git commit -m "fix"

# Meanwhile, main updated
git checkout main
echo "App v2" >> app.js
git add app.js && git commit -m "v2"

# Your task:
# 1. Feature branch main-এর সাথে update করুন (rebase)
# 2. 4টি messy commits → 2টি clean commits (interactive rebase)
# 3. PR-ready করুন

# Solution:
git checkout feature-user-auth

# Step 1: Update with main
git rebase main

# Step 2: Clean history
git rebase -i HEAD~4

# Editor:
pick abc123d WIP login
fixup def456g continue
pick ghi789j WIP signup
fixup jkl012m fix

# Message editor:
# feat(auth): implement login functionality
# feat(auth): implement signup functionality

# Step 3: Verify and push
git log --oneline --graph --all
git push origin feature-user-auth
```

---

## 📚 আরও শিখুন

### Related Phases:

- **[Phase 3: Conflict Resolution](Phase3_Complete.md)** - Merge এবং Rebase conflicts handle করা
- **[Phase 4: Recovery & History Cleanup](Phase4_Complete.md)** - Advanced history manipulation
- **[Phase 5: Team Simulation](Phase5_Complete.md)** - Real team workflow

### Commands চিট শীট:

**Cherry-Pick:**

```bash
git cherry-pick <commit-sha>              # Single commit
git cherry-pick <sha1> <sha2> <sha3>      # Multiple commits
git cherry-pick <sha1>^..<sha3>           # Range
git cherry-pick --continue                # After conflict
git cherry-pick --abort                   # Cancel
```

**Rebase:**

```bash
git rebase <branch>                       # Basic rebase
git rebase -i HEAD~n                      # Interactive rebase
git rebase --continue                     # After conflict
git rebase --abort                        # Cancel
git rebase --skip                         # Skip current commit
```

**Force Push (সাবধানে!):**

```bash
git push --force-with-lease origin branch # Safer force push
git push --force origin branch            # Dangerous!
```

---

## 🎓 সারসংক্ষেপ

### Cherry-Pick:

- ✅ Specific commits কপি করার জন্য
- ✅ Bug fixes multiple branches-এ apply করতে
- ⚠️ সংযতভাবে ব্যবহার করুন

### Rebase:

- ✅ Clean, linear history চান
- ✅ Personal feature branches update করতে
- ❌ Public/shared branches-এ NEVER!

### Golden Rule:

> **"If in doubt, use MERGE instead of REBASE!"**

---

**Practice করুন, ভুল করুন, শিখুন! 🚀**

Questions? এই topics নিয়ে আরও জানতে চান? জিজ্ঞাসা করুন! 💬
